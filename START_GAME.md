# PROTOCOL Manager 실행

1. ZIP을 새 폴더에 압축 해제합니다. 기존 저장소를 덮어쓰지 않아도 됩니다.
2. Node.js 22.13 이상을 설치합니다.
3. package.json이 있는 폴더에서 터미널을 열고 실행합니다.

```sh
npm ci
npm run dev
```

터미널에 표시된 로컬 주소(기본 http://localhost:5173, 사용 중이면 자동으로 다음 포트)를 브라우저로 여세요.
HTML 파일을 더블클릭하거나 기존 접근 거부 링크를 사용할 필요가 없습니다.

## 로그인 없이도 바로 플레이됩니다 (2026-09-15 실제 확인)

`npm run dev`는 `vinext` 개발 서버를 실행합니다. 기동 로그에 `Sites local sign-in: seedy@sites.test`가
찍히는데, 이는 로컬 전용 가짜 사용자로 자동 로그인된 것입니다 — 운영에서 쓰는 ChatGPT 계정 연동
(`oai-authenticated-user-id` 헤더)을 로컬에서 흉내 낸 것이며, 사용자가 별도로 로그인할 필요가
없습니다. 저장 API(`app/api/game/route.ts`)가 이 헤더 없이 401을 반환하는 것은 사실이지만,
`npm run dev`로 실행하는 한 이 401은 사용자 눈에 보이지 않습니다.

저장은 Cloudflare D1을 로컬 모드(`wrangler --local`)로 에뮬레이션해 `.wrangler/state`에 저장됩니다.
이 폴더는 `.gitignore`에 포함되어 있어 커밋되지 않으며, 실제 운영 사이트의 계정·저장 데이터와
완전히 분리되어 있습니다(운영 인증·데이터를 건드리지 않음).

**실제로 확인한 경로**: 새 커리어 생성(감독으로 부임) → 주간 훈련 확정 → 밴픽 → 경기 진행 →
브라우저 완전히 새 탭에서 재접속 → 동일한 슬롯의 진행 상황이 그대로 복원됨. 커리어 슬롯 1/2/3은
서버에서 완전히 분리되어 저장되며 섞이지 않습니다.

## 문제가 생기면

- 화면이 계속 로딩 중이면 터미널에 `Sites local sign-in` 줄이 찍혔는지 확인하세요. 없다면 `npm run dev`가
  아닌 다른 방식(예: `wrangler dev` 직접 실행)으로 띄운 것일 수 있습니다 — 그 경로는 로컬 자동 로그인이
  적용되지 않아 401이 그대로 화면에 노출됩니다.
- 저장이 안 되면 `.wrangler/state` 폴더 권한이나 디스크 공간을 확인하세요. 이 폴더를 지우면 로컬
  저장 데이터가 초기화됩니다(운영 데이터와는 무관).

수정 내용·검증·남은 한계는 docs/claude/BROADCAST_UPDATE.md와 docs/claude/PROTOCOL-Claude-Development-Orders.md에 있습니다.
공개 서버 배포는 별도이며 이 수정본에서는 배포하지 않았습니다.
