# DEVELOPMENT_ORDERS 진행 기록

`PROTOCOL-Claude-Development-Orders.md`(F01~F27)를 순서대로 진행한 실제 기록. 각 항목은 7절 보고 양식을 따른다.
완료로 적힌 항목도 "재구현 금지" 원칙에 따라 이미 충족된 부분은 다시 만들지 않는다 — 다음 세션은 이 파일과 최신 코드를 먼저 대조한다.

## F02 — PC·모바일 방송 화면 인수 · 부분 완료(데스크톱) / 모바일은 도구 제약으로 BLOCKED (2026-09-15)

**목표:** 1440×900/390×844/360×800에서 미니맵·상태창·중계·결과 동기화를 확인하고 "필수 동작"(t=0 정지 확인·1/2/4배속·슬라이더·다음 사건·완료 사건 클릭·끝으로 이동·종료 후 되감기·재시작·탭 비활성/복귀·선수 아이콘 키보드/터치·이미지 실패 폴백) 전부를 실제로 검증한다.

**사용자에게 달라진 경험:** 없음(코드 미변경) — 실제 동작을 재현 가능하게 검증만 했고, 검증 중 실제 결함은 발견되지 않았다.

**조사·검증 방법:** `resize_window`로 390×844/360×800을 시도했으나 `window.innerWidth`가 여전히 1920으로 유지됨을 JS로 직접 확인 — 이 환경은 실제 브라우저 창을 리사이즈하지 못하고 CDP 캡처가 고정 해상도에 묶여 있다(과거 D018/D020/D021/D022 세션에서도 동일하게 기록된 제약, 이번에도 재현됨). **모바일 두 크기는 실제 인수를 하지 못했다 — BLOCKED로 남긴다, 완료로 표시하지 않는다.**

대신 데스크톱 유효 폭(약 1568~1905px, 1440~1600 구간 포함)에서 `app/replay-theater.tsx`의 모든 "필수 동작"을 `javascript_tool`로 실제 DOM 이벤트를 발생시켜(좌표 클릭이 아니라 `dispatchEvent`로 React `onChange`/`onClick` 핸들러를 직접, 그러나 실제 컴포넌트 코드 경로로 구동) 하나씩 검증했다. rAF 기반 자동 재생은 이 자동화 환경에서 `document.hidden`이 false로 읽히는데도 실질적으로 스로틀링되어 실시간 흐름 관찰은 신뢰할 수 없었다(별도 발견 — 아래 "환경 특이사항" 참조). seek/버튼 기반 제어는 rAF에 의존하지 않아 안정적으로 검증됐다.

**실제로 실행한 검증과 결과 (전부 정상):**
- 시크 슬라이더 → 임의 지점(12:50)으로 정확히 이동, 점수·골드 갱신, 결과 미노출(스포일러 없음).
- 끝으로 이동(슬라이더 max) → RECAP 카드 노출, 실제 최종 점수(1:2) 공개, 구조물/KDA 전부 집계된 상태로 표시.
- 종료 후 되감기(슬라이더를 40%로) → RECAP 카드 다시 숨김(재노출 없음, 결과 미리보기 없음) — `BROADCAST_CONTINUATION.md`가 주장한 수정이 실제로 동작함을 확인.
- 완료된 사건 클릭(`.rt-history` 버튼, 예: "드래곤" 사건) → 해당 사건 시작 시각으로 정확히 이동.
- 재시작(처음부터 버튼) → t=00:00으로 복귀 + 자동 재생 재개, RECAP 카드 숨김 유지.
- 재생/일시정지 토글 → 상태 정상 전환(단, DOM 반영이 지연될 때가 있음 — 아래 환경 특이사항).
- 선수 아이콘: 코드 검토로 `role="button"`·`tabIndex={0}`·`onKeyDown`(Enter/Space) 확인 — 키보드 접근 이미 구현됨.
- 이미지 실패 폴백: `<image>`의 `href`를 존재하지 않는 URL로 직접 교체해 강제 실패시킴 → `onError`가 이미지만 `display:none` 처리하고 밑에 있던 약어 텍스트(`.rt-abbr`)가 정상 노출됨을 실측 확인.
- 콘솔 에러 0건. 데스크톱 유효 폭에서 `document.documentElement.scrollWidth===clientWidth`(가로 넘침 없음).

**환경 특이사항(버그 아님, 오탐 기록):** 처음에는 "끝으로 이동해도 재생 버튼 라벨이 계속 '일시정지'로 남는다"를 결함으로 의심했으나, 대기 시간을 늘리고 화면을 직접 스크린샷으로 재확인하자 실제 아이콘은 이미 Play(▷)로 정확히 바뀌어 있었다 — 이 환경에서 React 상태 갱신이 DOM에 반영되는 시점이 수백 ms 지연될 수 있다는 것이 원인. 실제 애플리케이션 결함이 아니므로 코드 수정 없음.

**정적 감사(모바일 폭을 실제로 못 봤으므로 보완):** `app/globals.css`에 이미 `max-width:1250/1050/767px` 전 구간 반응형 규칙과 `.rt-stage/.rt-map`(768px), `.rt-lineups`(420px) 미니맵 전용 규칙이 존재한다(이전 세션들이 만든 것, 이번에 새로 만들지 않음). 픽셀 단위로 눈으로 확인한 것은 아니므로 "완료"라 주장하지 않고, 구조적으로 좁은 화면 대응 코드가 있다는 사실만 기록한다.

**기준선 대비 변화:** 없음(엔진·표시 코드 모두 미변경).

**알려진 문제·미검증·외부 차단:** **BLOCKED(도구 제약)** — 390×844·360×800 실제 픽셀 검증은 여전히 불가능. 실기기 또는 이 자동화가 실제로 창 크기를 바꿀 수 있는 환경이 확보되기 전까지는 "완료"로 올리지 않는다. 터치 히트박스 겹침·모바일 전용 레이아웃 버그는 코드만으로는 완전히 배제할 수 없다.

**다음 작업 ID / 바로 실행할 첫 행동:** F03 — `combat.resource`/`lead[]`/표시 `goldA·goldB`/POG/킬 피드/구조물 스냅샷의 생성·소비 경로를 감사해 이중 지급이나 표시 골드 임의 증가가 있는지 확인한다.

## F03 — 경기 사건과 자원 원장의 단일화 · 감사 완료, 의도적으로 통합 보류 (2026-09-15)

**목표:** `combat.resource`/`lead[]`/표시 `goldA·goldB`/POG/킬 피드/구조물 스냅샷이 같은 원본에서 파생되는지 감사하고, 이중 지급·표시 골드 임의 증가만 고친다(이미 단일화된 부분은 보존).

**사용자에게 달라진 경험:** 없음 — 코드 동작 변경 없음, `lib/game.ts`에 설명 주석만 추가(아래).

**감사 결과 (4개 완료 조건 개별 확인):**
1. **KDA = 원본 처치·어시** — ✅ 참(이번 세션 F-KDA 작업에서 실측 검증: 6경기 시뮬레이션 누적 처치 1484 = 누적 데스 1484). POG도 `e.combat.kills`/`fight.contrib`를 직접 읽어 별도 재계산이 없다(D019~D020에서 이미 이중가산 제거 확인, 이번엔 재검증만).
2. **구조물 중복 보상 0** — ✅ 참. `lib/simulation/combat.ts` `resolveSiege`의 `s3[lane]`(0~3 진행도)·`st.baseTurrets`·`st.nexus`는 `MatchState`에 영구 보존되고, 골드 지급(`add()`)은 `for(let k=0;k<step;k++)` 루프 안에서 그 사건에 새로 오른 진행도만큼만 실행된다(`step=Math.min(budget,2,3-s3[lane])`) — 이미 부순 구조물은 진행도 상한 때문에 물리적으로 다시 지급받을 수 없다.
3. **리플레이 임의 시각에서 미래 효과 0** — ✅ 참. F02 검증에서 실측: 임의 시각 시크·끝으로 이동·되감기 전부 그 시점까지 집계된 상태만 노출, 미래 사건 유출 없음.
4. **화면 팀 골드 = 정의된 개인 골드 합** — ❌ 거짓, 그리고 **의도적으로 지금 고치지 않는다.** 아래 근거.

**핵심 발견:** `lib/simulation/combat.ts`의 `Combatant.gold`(주석: "개인 자원, 세트 누적, 기준 0")는 처치·어시스트·오브젝트·구조물마다 `add()` 한 곳에서만 갱신되는 **진짜 개인 골드 원장**이다. `game.ts`의 `lead[s]+=cb.resource[s]`가 정확히 이 값의 사건별 델타를 미러링한다(단일 출처, 문제 없음). 반면 화면에 그려지는 `e.goldA/e.goldB`(GoldGraph, "글로벌 골드 차")는 **완전히 별도의 `flavor` 난수 스트림으로 만드는 팀 단위 가산 곡선**(`gA+=passive+(waIn?gain:gain*.34)` 등)이며 `Combatant.gold`를 전혀 읽지 않는다 — F03이 정확히 지목하는 "단일 원장이 아닌 곳"이다. 다만 방향(`waIn`, 그 사건의 실제 승자)은 항상 일치시켜 가짜 승자를 만들지는 않는다 — "이중 지급"은 아니고 "병행 생성"이다.

**왜 지금 통합하지 않았는가(중요한 판단):** `Combatant.gold`는 전투 사건에서만 오르고 **파밍/웨이브 수입이 전혀 없다**(F09 미착수) — 그래서 실제 LoL 골드보다 훨씬 작고 들쭉날쭉하다. 지금 그대로 "진짜 골드 합"으로 바꿔치기하면: (a) `tests/combat.test.mjs` 8a("미확보 오브젝트 표시 골드 균등" — 확보 실패 시 A/B 표시 골드가 거의 같아야 한다는 정보 유출 방지 안전장치)가 실제로 깨진다(미확보 교전 중에도 실제 처치가 있으면 골드가 한쪽으로 크게 기울 수 있음), (b) 그래프가 매끈한 성장 곡선 대신 전투 사건에만 반응하는 계단식으로 바뀌어 오히려 덜 현실적으로 보인다. F03 지시문 자체가 "실제 전체 골드로 확장할 때는 초기 골드·자연 수급·파밍·처치·오브·구조물의 포함 범위를 먼저 정의한다"고 명시 — 파밍 수입 없이는 그 범위 정의가 불완전하다. **결론: 이 통합은 F09(라인 웨이브·귀환·파밍 경제) 이후로 미루는 것이 옳다.** 억지로 지금 맞추는 것은 "결과-영향 변경을 충분한 검증 없이 강행"하는 것과 같은 종류의 실수라고 판단했다(D021이 같은 이유로 엔진 재설계를 다음 세션으로 미룬 선례와 같은 논리).

**변경 파일:** `lib/game.ts`(`simulateSet` 상단에 flavor/실제 골드 계약을 설명하는 주석 5줄 추가 — 코드 동작 불변, 섹션 6 "outcome/combat/flavor/narration 난수의 역할을 문서화한다" 요건 충족).

**실제로 실행한 검증과 결과:** tsc 0 에러, `tests/management.test.mjs` PASS(코드 동작 미변경이므로 나머지 스위트는 재실행 불필요 — 순수 주석 diff).

**기준선 대비 변화:** 없음.

**알려진 문제·미검증·외부 차단:** 없음(BLOCKED 아님) — 이건 "못 함"이 아니라 "지금 하면 더 나빠져서 의도적으로 미룸"이다. F09 완료 후 재검토 대상으로 `BACKLOG.md`류 문서에도 남겨야 한다(다음 세션 권장).

**다음 작업 ID / 바로 실행할 첫 행동:** F04 — 오브젝트 준비 구간에서 한 번 멈춰 "싸움 준비/반대편 이득/정비" 감독 지시를 받는 첫 완결 기능(단계 진행 엔진 + `MatchSession` 저장 계약). 가장 위험도가 높은 신규 기능이므로 설계 계약부터 명확히 하고 시작한다.

## F01 — 실행·저장 왕복 · 완료 (2026-09-15)

**목표:** 새 커리어 생성→훈련→밴픽→경기→저장→새로고침→복원이 실제로 되는지 확인하고, 안 되면 고친다.

**사용자에게 달라진 경험:** 없음(기능 변경 아님) — 다만 `START_GAME.md`가 실제 동작과 일치하도록 정정됐다. 이전 문서는 "401이 날 수 있다"는 불확실성만 남겼는데, 실제로는 `npm run dev`(vinext) 자체가 로컬 전용 가짜 사용자로 자동 로그인하므로 사용자가 아무것도 더 할 필요가 없다.

**조사 결과:** `app/api/game/route.ts`는 `oai-authenticated-user-id` 헤더가 없으면 401을 반환하는 것이 사실이다. 그러나 `scripts/run-framework.mjs`가 실행하는 `node_modules/vinext`의 dev 서버가 기동 시 `Sites local sign-in: seedy@sites.test`를 출력하며 이 헤더를 로컬 요청에 자동으로 주입한다(Sites 플랫폼의 로컬 개발 편의 기능). 저장은 `wrangler --local`이 `.wrangler/state`에 D1을 에뮬레이션하며 이 경로는 `.gitignore`에 이미 포함되어 있어 운영 데이터와 분리된다. 즉 **F01이 요구하는 조건은 이미 충족되어 있었다** — 코드 결함이 아니라 문서의 불확실성이 문제였다.

**변경 파일:** `START_GAME.md`(재작성 — 로컬 자동 로그인 동작·저장 위치·분리 보장을 실제 확인한 그대로 기술, 문제 발생 시 진단 순서 추가).

**실제로 실행한 검증과 결과:**
- 커리어 3(빈 슬롯)에서 Gen.G로 신규 생성 → "주간 훈련 확정" 화면 정상 진입, 시작 자금 10.00억/샐러리캡 25.00억 정상.
- 완전히 새 브라우저 탭으로 `http://localhost:5174/`(vinext가 5173 사용 중이라 자동 전환한 포트) 재접속 → 커리어 1(T1, KT전 진행 중, 자동 저장됨 표시) 상태가 그대로 복원.
- 커리어 드롭다운으로 슬롯 1/2/3 전환 → 각각 T1/BRO/Gen.G로 완전히 분리되어 있고 리그 순위표의 "MY TEAM" 강조도 슬롯별로 정확히 바뀜(슬롯 혼동 없음).
- 콘솔 에러 없음.
- 중복 요청/오래된 revision 방지는 `app/api/game/route.ts`의 기존 코드 리뷰로 확인(commandId+fingerprint 중복 무시, `revision` 비교 후 UPDATE 실패 시 409 반환) — 별도 수정 불필요, 브라우저 재현은 하지 않음(코드가 이미 원자적 CAS라 결정적).

**기준선 대비 변화:** 없음(엔진·저장 스키마 미변경).

**알려진 문제·미검증·외부 차단:** 없음. F01은 BLOCKED가 아니라 실제로 완료 상태였다.

**다음 작업 ID / 바로 실행할 첫 행동:** F02 — 실행 중인 dev 서버에서 Claude in Chrome으로 1440×900/390×844/360×800 세 크기의 방송 화면을 인수한다. 과거 세션 기록(D018~D022)에 "CDP 스크린샷이 1568px로 고정되고 `resize_window`가 무시된다"는 반복된 도구 제약이 있으므로, 이번에도 막히면 즉시 BLOCKED로 기록하고 1440×900처럼 되는 크기부터 진행한다.

## F04 — 경기 중 결정 가능한 단계 진행 엔진 첫 슬라이스 · 완료 (2026-09-15)

**목표:** "한 번의 play로 simulateSet을 끝까지 계산"하는 구조를 버튼만 얹어 흉내 내지 말고, 오브젝트 준비 구간에서 실제로 한 번 멈춰 감독 지시("싸움 준비/반대편 이득/정비")를 받아 이후 사건에만 반영하는 첫 완결 기능을 만든다.

**사용자에게 달라진 경험:** 밴픽을 마치고 "세트 경기 시작"을 누르면 이제 세트가 즉시 끝나지 않는다. 라인전 3사건(탑·미드·바텀) 결과를 먼저 보여주고, 그 결과를 바탕으로 오브젝트 구간 작전을 셋 중 하나로 고르게 한다: **싸움 준비**(다음 한타 유리, 오브젝트 확보엔 안 도움)·**반대편 이득**(오브젝트 확보 유리, 한타 준비는 덜 됨)·**정비**(중립, 기존과 동일). 고른 뒤에만 세트가 끝까지 계산되어 리캡으로 넘어간다. 방송 화면(재생)은 기존과 동일하게 동작한다 — 사건 자체가 더 생겼을 뿐 재생 방식은 안 바뀐다.

**설계 계약(왜 이렇게 구현했나 — 위험 판단이 핵심):**
- `simulateSet`의 9구간 스켈레톤·닫힌 클로저(rollFight/finalize/doSiege, lead[]/advantage/pressureA·B/cs 등 8개 이상의 상호 의존 지역 변수)를 실제로 "일시정지 가능한 두 단계"로 쪼개는 방법을 먼저 설계했지만, RNG 클로저 상태·`MatchState`(cs) 전체를 직렬화해 서버 간 왕복시키는 방식은 이 세션에서 검증 없이 강행하기엔 회귀 위험이 너무 크다고 판단했다(D022가 같은 함수 영역에서 정비 버퍼 하나만 건드려도 한타 34%가 붕괴하는 심각한 회귀를 낸 전례가 있음).
- 대신 **"같은 시드로 두 번 계산해도 라인전 3사건은 항상 바이트 단위로 동일하다"는 `simulateSet`의 순수성**을 이용해 위험을 제거했다: `{type:'play'}`가 `simulateSet(g,m)`(directive 없음=완전 중립)을 한 번 돌려 라인전 3사건만 "미리보기"로 저장하고 `TACTICAL` 단계로 멈춘다. 사용자가 지시를 고르면 `{type:'tacticalChoice'}`가 `simulateSet(g,m,directive)`를 **처음부터 다시** 계산하고, 커밋 직전에 `JSON.stringify`로 "라인전 3사건이 미리보기와 정확히 같은가"를 런타임에 검증한다(다르면 저장하지 않고 에러) — 이게 "과거 사건 불변"을 지키는 실제 안전장치다.
- `directive`는 `simulateSet`의 access 계산 한 곳(`rollFight` 호출부)에만 작은 상수(±1.2, 조합 효과 ±3보다 작게)를 더하는 방식으로 넣었다 — 기존 클로저·루프·POG·KDA·골드 계산은 단 한 줄도 재구성하지 않았다. `directive` 생략 시(기존 모든 호출: `autoMatch`, 전체 회귀 테스트) 동작이 100% 이전과 같음을 보장한다(코드로 보장 — `tacticalAccess()`가 `!directive`면 무조건 0 반환).
- `case 'play'`(사용자의 현재 활성 매치 전용)만 바꿨다. `autoMatch`(다른 팀·플레이오프·국제대회 자동 진행)는 완전히 별도 코드 경로라 전혀 손대지 않았다 — AI 시즌 진행 속도·결과에 영향 없음.

**변경 파일 / 데이터 계약:**
- `lib/game.ts`: `TacticalChoice`/`TacticalState` 타입, `Match.tacticalState?`, `Game.phase`에 `'TACTICAL'` 추가. `simulateSet(g,m,directive?)` — `tacticalAccess()` 헬퍼로 access에 작은 가산. `case 'play'`는 이제 미리보기만 계산해 `TACTICAL`로 전환. `case 'tacticalChoice'` 신설(검증 후 커밋, 기존 'play'와 동일한 마무리).
- `app/manager.tsx`: `TacticalPrompt` 컴포넌트 신설(라인전 3사건 + 3개 선택 버튼 + 안내 문구). 매치 스테퍼 5단계로 확장(전략 준비/밴픽/**작전 지시**/경기/리캡). `nextLabel`/`next()`에 TACTICAL 분기 추가.
- `app/globals.css`: `.tactical-*` 스타일(모바일 1열 폴백 포함, 기존 767px 브레이크포인트 재사용).
- `tests/engine.test.mjs`: `next()` 헬퍼에 TACTICAL 분기 추가 — prepare/trade/regroup을 세트 수로 순환시켜 세 지시 전부를 실제로 태운다. 커밋 직후 "라인전 사건=미리보기와 완전히 같음"을 `assert.deepEqual`로 검증(테스트 차원에서 한 번 더 확인).

**실제로 실행한 검증과 결과:**
- tsc 0 에러. `npm run build` 성공.
- `tests/engine.test.mjs`: **PASS** — 3 커리어 × 4시즌, 1086 결정적 사용자 세트 전부가 새 TACTICAL 경로(각 세트마다 play→tacticalChoice 2commands)를 거쳤고, "커밋된 세트의 라인전 3사건=지시 전 미리보기와 완전히 동일" 안전장치가 매번 통과. season/hall/salary 등 기존 불변식도 전부 유지.
- `tests/{management,combat,composition,ability,narration,replay}.test.mjs`: 전부 **PASS**(내가 손대지 않은 영역 — `autoMatch`/`simulateSet` 직접 호출 경로가 전혀 영향받지 않음을 별도로 재확인).
- 브라우저 실측(Claude in Chrome, T1 vs DK 실제 시즌 경기): 밴픽 완료 → "세트 경기 시작" → **"오브젝트 준비 — 작전을 지시하세요." 화면에 라인전 3사건(탑/미드/바텀) 실측 표시** → "싸움 준비" 클릭 → 서버 상태 확인 결과 `phase:'RECAP', scoreA:1, sets.length:1`로 정상 커밋 → 홈 화면에 "SET 1 · T1 1:0 DK · 리캡 확인" 정상 표시. 콘솔 에러 0건.

**기준선 대비 변화(엔진 변경):** `directive` 미지정 시 전부 동일(코드·테스트로 보장). `directive` 지정 시(신규 경로만) access에 최대 ±1.2 추가 — 조합 효과(±3)보다 작게 의도적으로 설계했다. 대규모 시드 스윕을 통한 밸런스 영향 측정은 하지 않았다(신규 선택적 경로이고 기존 자동 진행 매치는 전혀 타지 않으므로 시즌 전체 밸런스에는 영향 없음 — 사용자가 매 세트 직접 다른 지시를 고를 때만 작은 영향이 누적됨. 다음 세션에서 F15 도구로 측정 권장).

**알려진 문제·미검증·외부 차단:** 없음(BLOCKED 아님). 다만 이번 슬라이스는 F04 전체가 아니라 "첫 범위"만 구현한 것 — 전체 실시간 조작 UI, 진짜 mid-closure 일시정지/재개(서버 재시작 후에도 RNG 클로저 자체를 복원하는 방식)는 의도적으로 다음으로 미뤘다. 지금 방식(재계산+검증)도 "중간 저장·새로고침·재개" 요구를 충족하지만(라인전 3사건이 결정적이므로), 향후 F08(지속 이동)처럼 라인전 자체가 더 복잡해지면 이 "재계산" 전략의 비용이 커질 수 있다 — 그때는 진짜 체크포인트 직렬화를 재검토해야 한다.

**다음 작업 ID / 바로 실행할 첫 행동:** F05 — 챔피언 프로필 차별화(대표 20종부터). 또는 F12(경기 중 감독 지시 확장)로 이번에 만든 TACTICAL 기반을 더 발전시키는 것도 자연스러운 다음 단계.

## F05 — 챔피언마다 다른 경기 성격 · 대표 20종 첫 슬라이스 완료 (2026-09-15)

**목표:** 140개 챔피언 ID는 유지한 채, 기존 "태그 2개(engage/peel/poke/scale) → 특성 파생"의 한계를 보완한다. 대표 20종부터 수동 프로필로 검수하고 점진적으로 확대한다.

**사용자에게 달라진 경험:** 밴픽 화면의 조합 분석(추천 3개·CompositionPanel)과 리캡의 조합 근거 문구가 이 20개 챔피언이 포함된 픽에서 더 정확해진다. 예: 피오라를 포함한 조합이 더 이상 "포킹 조합"으로 잘못 분류되지 않는다. 나머지 120종은 이전과 완전히 동일하게 동작한다(회귀 없음).

**조사한 실제 문제(재현 확인):** `lib/champions.ts`를 실제로 읽어보니 **피오라(Tfiora)가 `['scale','poke']` 태그**를 갖고 있었다 — 근접 듀얼리스트인데 "원거리 견제(poke)" 태그가 붙어, 기존 `champProfile()`이 태그만으로 poke=1·range+.8을 부여하고 있었다. 이게 F05가 지목한 "사이드 운영 챔피언이 잘못된 포킹 특성 때문에 포킹 조합의 중심으로 평가"의 실제 사례였다(가정이 아니라 코드에서 직접 확인).

**설계 결정:**
- **완전히 새로 만들지 않았다.** `lib/balance/composition.ts`의 `champProfile()`에 `CURATED:Record<string,CompProfile>` 오버라이드 표를 추가 — 이 표에 있는 id는 수동 값을, 없는 120종은 기존 태그 파생 계산을 그대로 쓴다(단일 함수, 단일 폴백 — "UI·AI·엔진이 동일 프로필을 사용한다" 요건을 구조적으로 만족).
- **12개 후보 중 "첫 범위" 권고(4~6개)보다 넓게, 기존 8축을 그대로 쓰고 신설 1축만 추가**: `sideline`(사이드 라인 단독 운영·스플릿) — 기존 4태그 체계엔 이 개념 자체가 없었다(포킹·이니시·보호·성장 중 아무것도 "혼자 사이드를 압박한다"를 담지 못함). 축을 늘리는 대신 기존 8축 값을 챔피언별로 정확히 다시 매겨 태그-뭉뚱그림 문제를 고치는 데 집중했다.
- **대표 20종 선정 기준**: 역할당 4명, "기존 태그로는 정체성이 왜곡되는" 사례를 우선했다(피오라·카밀·베인·페이커의 상징 그레이브즈·파이크는 전부 poke/engage 태그가 실제 킷과 어긋나 있었다). TOP: 피오라·카밀·말파이트·잭스 / JGL: 바이·자크·그레이브즈·아이번 / MID: 제라스·야스오·아지르·제드 / ADC: 케이틀린·베인·징크스·칼리스타 / SUP: 쓰레쉬·룰루·파이크·유미.
- **출처**: 클로드 학습 데이터 기준 공식 챔피언 스킬셋 정체성(검수일 2026-09-15) — 특정 패치 수치가 아니라 "원거리 견제 수단이 있는가/근접인가/스플릿 생존기가 있는가" 같은 패치에 잘 안 변하는 안정적 정체성만 반영했다(패치별 밸런스 수치를 실측 승률인 것처럼 표시하지 않는다는 원칙 유지).
- **`draftEffects`의 `obj`(오브젝트) 채널에만 `sideline` 항을 추가**(`tanh` bounded, ±3 상한 안에서). 라인·한타에는 넣지 않았다 — "사이드에 있다"는 것 자체가 그 교전엔 안 낀다는 뜻이라 이중 반영을 피했다.

**변경 파일 / 데이터 계약:**
- `lib/balance/composition.ts`: `CompProfile`에 `sideline` 축 추가. `CURATED` 20종 표(챔피언별 8+1축 명시값 + 실제 킷 근거 한 줄 주석). `champProfile()`이 `CURATED` 우선 조회. `aggregateRaw`에 `sideline` 집계 추가. `draftEffects`의 `obj` 공식에 `0.45*tanh(sideline차/3)` 항 추가.
- `tests/composition.test.mjs`: import에 `compositionPlan` 추가. 섹션 6 신설 — 피오라 poke=0·sideline>1 직접 검사, 미검수 챔피언은 태그 폴백 유지 확인, 근접 듀얼리스트 5인 조합이 더 이상 'poke' 스타일로 분류되지 않음, 프론트라인 열세 조합도 sideline 격차 덕분에 obj 채널이 fight 채널보다 유리해짐(설계 의도 직접 증명).

**실제로 실행한 검증과 결과:**
- tsc 0 에러, `npm run build` 성공.
- `tests/composition.test.mjs`: **PASS**(신설 섹션 6 포함) — 6000시드 통계 비교(섹션 4, 전체 140종 대상 동적 최적 조합 선택)도 그대로 통과, 즉 20종 재정의가 기존 "앞라인 조합이 포크 조합 상대 한타 우위" 같은 큰 방향성 검증을 깨지 않았다.
- `tests/engine.test.mjs`: **PASS** — 3커리어×4시즌, 1093 결정적 세트(F04의 TACTICAL 경로 포함) 전부 통과, season/salary/mastery 불변식 유지.
- `tests/{combat,ability,management,narration,replay,spatial,director,broadcast}.test.mjs`: 전부 **PASS**.

**기준선 대비 변화(확률 채널 변경):** 20개 챔피언의 `champProfile` 반환값이 바뀌었고 `obj` 공식에 새 항이 생겼다 — 이 20개 챔피언이 실제로 픽되는 경기에서만 `lane/obj/fight` 확률 채널이 이전과 달라진다(±3 상한은 그대로 유지). **대규모 시드 스윕으로 승률 영향의 크기를 따로 측정하지는 않았다** — `draftEffects` 자체의 대칭성·상한·조합 민감도는 기존 회귀 스위트(섹션 2·4)가 이미 넓게 검증하고, 신설 축도 같은 클램프 규칙을 따르므로 구조적 위험은 낮다고 판단했지만, "이 정도 영향력이 딱 적절한 크기인가"는 F15 밸런스 도구가 아직 없어 정량적으로 못 박지 못했다.

**알려진 문제·미검증·외부 차단:** 없음(BLOCKED 아님). 나머지 120종은 여전히 태그 2개 파생 폴백이다 — "점진적으로 전체로 확대"가 남은 작업(다음 세션들이 `CURATED`에 계속 추가하면 됨, 구조는 이미 갖춰짐). `CompStyle`(poke/engage/protect/scale) 4종에 "사이드/스플릿" 5번째 라벨을 별도로 추가하지는 않았다(스코프를 "잘못된 poke 오분류 고정" 수정에 집중) — UI에 사이드 운영 조합을 명시적으로 라벨링하고 싶으면 후속 작업으로 분리해야 한다.

**다음 작업 ID / 바로 실행할 첫 행동:** F06 — 상성·시간대가 있는 조합 시스템(포킹/강제진입/보호/픽매복/사이드 압박). F05가 방금 만든 `sideline` 축이 F06의 "사이드 압박" 유형과 바로 연결된다.

## F06 — 상성·시간대가 있는 조합 시스템 · 픽/매복 원형 신설 (2026-09-15)

**목표:** 포킹·강제진입·보호·픽/매복·사이드 압박 5가지를 지원하고, "강제 진입은 항상 포킹을 이긴다" 식 전역 가위바위보가 아니라 시야·거리·진입 수단·보호·성장 시점 조건이 충족될 때만 유리해지게 한다. 체력 모델이 없으면 실제 체력처럼 표시하지 않는다.

**조사 결과(중요 — 예상보다 많이 이미 돼 있었다):** 코드를 실제로 읽어보니 F06의 핵심 "행동 연결" 요구사항은 **이미 D008(COMP-01 단계 2)에서 구현돼 있었다**:
- `lib/simulation/combat.ts`의 `resolveTeamfight`가 `battlePlan()`을 실제로 소비한다 — `plan.preparation`(포킹 압박)·`plan.entry`(진입)가 한타 성사 확률(`engageP`)과 승패 확률(`favWins`)에 직접 들어가고, `plan.protectA/B`가 서포터 보호 판정 확률을 실제로 올린다. `growth`는 경기 시계 기반 성장 시점 가중.
- 필드명이 이미 "준비 압력"(preparation)이라 실제 체력처럼 표시하지 않는다는 요구도 이미 충족.
- `draftEffects`의 `pokeEdge`/`diveEdge`가 이미 "상대 진입이 약할 때만 포킹 유효/상대 보호가 약할 때만 다이브 유효" 조건부 로직으로 되어 있었다(전역 상성 보너스 아님).
- `app/composition-panel.tsx`가 이미 단일 라벨이 아니라 `scores` 객체 전체(견제/진입/보호/성장)를 UI에 노출하고 있었다 — "혼합 강점·약점을 보여라" 요구도 부분 충족.

**진짜 빠진 것 하나:** "픽/매복"이 통째로 없었다 — `CompStyle`에 값 자체가 없고, `engage`(팀 전체를 강제로 몰아넣음)와 "장거리 구속으로 교전 전 한 명만 고립"은 실제로 다른 메커니즘인데 구분이 안 돼 있었다. 이번 세션은 이 하나에 집중했다.

**변경 파일 / 데이터 계약:**
- `lib/balance/composition.ts`: `CompProfile`에 `pick` 축 신설(F05의 `sideline`과 같은 패턴). `CURATED` 21종(F05의 20종 + 신규 블리츠크랭크) 전부에 `pick` 값 추가 — 쓰레쉬(.9)·파이크(1.2)·블리츠크랭크(1.1)가 최상위(실제 후크·장거리 처형 스킬 보유), 나머지는 0에 가까움. `draftEffects`에 `pickEdge=a.pick*(1-min(1,b.peel/2.5))-b.pick*(1-min(1,a.peel/2.5))`(다이브와 동일한 "상대 보호 약할 때만 유효" 조건 구조) 신설, `lane` 채널에만 더함(한타·오브젝트엔 안 더함 — 이미 끊긴 인원차는 뒤따르는 사건에 자연히 반영되므로 이중 반영 금지). `compositionPlan`이 `pick`을 5번째 `CompStyle`로 인식(`scores.pick` 노출, 전용 goal/risk 문구).
- `app/composition-panel.tsx`: 라벨 맵에 `pick:'픽'` 추가(단일 지점이라 한 줄 수정으로 UI 반영 확인).
- `tests/composition.test.mjs`: 섹션 7 신설 — 같은 픽 조합이라도 상대 보호가 강해지면 `lane` 이득이 줄어듦을 직접 검증(전역 보너스가 아니라는 완료 조건을 정면으로 증명), 픽 특화 조합이 `compositionPlan`에서 실제로 `'pick'` 스타일로 인식됨을 확인.

**실제로 실행한 검증과 결과:**
- tsc 0 에러, `npm run build` 성공.
- `tests/composition.test.mjs`: **PASS**(신설 섹션 7 포함, 조건부 이득 감소를 실측으로 증명).
- `tests/engine.test.mjs`: **PASS** — 3커리어×4시즌, 1101 결정적 세트.
- `tests/{combat,ability,management,narration,replay,spatial,director,broadcast}.test.mjs`: 전부 **PASS**.
- 브라우저 실측(Claude in Chrome, T1 시즌 밴픽 화면): 조합 분석 패널에 "견제·진입·보호·성장·**픽**" 5개 축이 실제로 렌더링됨을 스크린샷으로 확인. 콘솔 에러 0건.

**기준선 대비 변화:** `pick` 축이 `lane` 채널에 새 항으로 들어갔다(±3 상한 유지). 21개 챔피언(신규 블리츠크랭크 1명 포함)의 프로필이 갱신됐다. F05와 동일하게 대규모 시드 스윕으로 영향 크기는 별도 측정하지 않았다.

**알려진 문제·미검증·외부 차단:** 없음. "시야가 충족될 때"라는 F06의 조건 예시는 문자 그대로는 못 넣었다 — 조합(composition.ts)은 픽 선택 id만 보는 순수 함수라 실제 플레이어 시야 스탯(`visEdge`, game.ts에만 있음)에 접근하지 못한다. 대신 같은 역할을 하는 "상대 보호(peel) 약함" 조건으로 구현했다(다이브와 동일 패턴) — 의미상 F06이 원한 "조건이 충족될 때만 유리"는 만족하지만, 조건의 종류(시야 vs 보호)는 다르다는 점을 정확히 남긴다. 사이드(F05)·포킹·진입·보호·성장·픽까지 6개 축이 다 갖춰졌으니, 다음은 이 축들을 활용한 F07(AI 밴픽 고도화)이 자연스럽다.

**다음 작업 ID / 바로 실행할 첫 행동:** F07 — 사람이 읽을 수 있는 AI 밴픽(위협 제거 밴·핵심 조합 선점·상대 응수 1단계 탐색). 지금 6축(견제/진입/보호/성장/사이드/픽) 프로필을 그대로 판단 근거로 쓸 수 있다.

## F07 — 사람이 읽을 수 있는 AI 밴픽 · 상대 응수 1단계 탐색 신설 (2026-09-15)

**목표:** 위협 제거 밴·핵심 조합 선점·상대 응수·flex 실제 배치·미완성 조합 보완을 판단하는 AI 밴픽. 확정 안 된 상대 픽을 미리 읽지 않는다. 추천·자동 픽이 같은 합법성 검사를 쓴다.

**조사 결과:** `lib/game.ts`의 `aiPick`/`draftRecommendations`를 실제로 읽어보니 이미 상당 부분 구현돼 있었다: 위협 제거 밴(`rate(opp,c.id)*2` — 상대 숙련도가 높은 챔피언을 우선 밴), flex 실제 배치 가능성(`draftRecommendations`가 `ROLE_PERMS` 전수로 최적 배치를 찾음), 미완성 조합 보완(`open` 라인 보너스), 추천/자동픽이 이미 `legalDraftCandidates()` 하나를 공유. **완전히 빠진 것은 "첫 범위"가 명시한 상대 응수 1단계 탐색**이었다 — 기존 `aiPick`은 순전히 "지금 상태에서 내 조합이 얼마나 좋아지는가"만 보고, "내가 이걸 고르면 상대가 다음에 무엇으로 되받아치는가"는 전혀 고려하지 않았다.

**구현:**
- `lib/game.ts`에 `bestOpponentReply(g,ds)` 신설 — 순수 함수, rng 미소비. "만약 지금까지의 실제 확정 상태에서 상대가 다음 합법 수 중 자신에게 가장 좋은 것을 고른다면 그 이득이 얼마인가"를 `draftFitScore`(이미 있던 함수, 순수·결정적)로 측정한다. **재귀 없음(정확히 1단계)**, 합법성은 `legalDraftCandidates`를 그대로 재사용 — 추천/자동픽/이 탐색이 전부 같은 검사를 쓴다.
- `aiPick`: 기존 점수 계산(불변)으로 전체 후보를 1차 채점 → 상위 5개(시간 예산)만 각각 "이걸 고른 가상 상태"를 만들어(`structuredClone`) `bestOpponentReply`로 응수 페널티를 적용 → 페널티 반영 후 최고점을 최종 선택. 노이즈(`rng()*noise`)는 기존 1차 채점에서만 쓰고 탐색 자체는 rng를 소비하지 않는다 — **기존 draft rng 스트림 호출 횟수·순서 불변**(표시 수정 원칙과 동일한 이유로 결과-영향 최소화).
- **성능 안전장치**: 상대 응수 탐색은 **사용자 실제 매치**(`m.a===g.teamId||m.b===g.teamId`)에서만 돈다. 처음 구현해 전체 테스트를 돌렸을 때 `autoMatch`(사용자가 보지 않는 리그 전체 자동 진행, 시즌당 수백 개 드래프트)까지 비싸게 만들어 `engine.test.mjs`가 눈에 띄게 느려지는 것을 실측으로 발견 — 배경 시뮬레이션 품질은 F07의 목적이 아니므로 사용자 매치로 범위를 좁혔다(회귀 없음, `scored[0]`는 안정 정렬로 기존 "첫 최고점"과 동일한 타이브레이크).

**변경 파일:** `lib/game.ts`(`bestOpponentReply` 신설, `aiPick` 상위 5개 후보에 응수 탐색 적용 + 사용자 매치 한정 가드).

**실제로 실행한 검증과 결과:**
- tsc 0 에러, `npm run build` 성공.
- `tests/engine.test.mjs`: **PASS** — 3커리어×4시즌, 1067 결정적 세트(사용자 매치 전부 탐색 경로를 탐). 처음엔 배경 매치까지 느려지는 걸 발견해 위 가드를 추가, 재실행 후 정상 시간(약 4분, 이전 세션들과 비슷한 수준)으로 복귀 확인.
- `tests/{composition,ability,combat,narration,replay,spatial,director,broadcast}.test.mjs`: 전부 **PASS**(관련 없는 스위트도 회귀 없음 재확인).
- 브라우저 실측(Claude in Chrome): T1 시즌 밴픽을 끝까지 진행(사용자 픽 + AI 상대 응답 전부) — 드래프트 정상 완료, 콘솔 에러 0건.

**기준선 대비 변화:** `aiPick`의 최종 선택 로직만 바뀌었다(배경 자동 매치는 가드로 완전히 이전과 동일). 승부 결정 자체(어느 팀이 이기는가)는 `simulateSet`의 확률·전투 로직과 무관해 바뀌지 않는다 — 바뀐 건 "AI가 어떤 챔피언을 고르는가"뿐이고, 대규모 시드 스윕으로 그 선택 변화가 실제 승률에 미치는 크기는 측정하지 않았다(F15 부재, 반복 기록).

**알려진 문제·미검증·외부 차단:** 없음. `draftRecommendations`(사용자에게 보여주는 추천 3개)에는 이번엔 응수 탐색을 넣지 않았다 — "AI가 무엇을 고를지"와 "사용자에게 무엇을 추천할지"는 분리된 함수라, 추천에도 넣으려면 별도 UI 문구("이 픽을 고르면 상대가 이렇게 받아칠 수 있음")까지 설계해야 해서 범위를 좁혔다. 다음 세션이 원하면 `bestOpponentReply`를 그대로 재사용할 수 있다.

**다음 작업 ID / 바로 실행할 첫 행동:** F08 — 열 명 모두 지속적인 목적을 가진 이동(F04 세션 진행 엔진과 자연스럽게 이어짐). 또는 F15(밸런스 검증 도구) — F05~F07에서 누적된 "대규모 시드 스윕 미실시" 부채를 갚을 도구가 없다는 지적이 세 번 반복됐다.

## F08 — 열 명 모두 지속적인 목적을 가진 이동 · 이미 완료 확인 (2026-09-15)

**목표:** 엔진에 현재 위치·목적·출발·예상 도착·변경 이유를 가진 행동 계획을 두고, 라인 관리·정글 동선·시야 지원·합류·귀환·부활 후 복귀가 사건 사이에도 이어지게 한다. AI는 완성된 미래 사건의 참가자 명단을 읽어 미리 이동시키지 않는다(재생 구성과 현재 경기 AI 분리).

**조사 결과: 재구현 없이 코드로 직접 검증만 했다 — 이미 되어 있었다.** F01처럼 이번에도 "문서가 불확실했을 뿐 코드는 이미 요구 조건을 충족"하는 경우였다. 확인한 근거:
1. **`lib/simulation/arena.ts`**(이번 세션 전, codex/match-broadcast 작업이 신설) — 엔진과 재생이 **같은 좌표계·같은 경로 그래프**(`MAP`/`WALK`, Dijkstra)와 **같은 이동 속도 함수**(`travelSeconds`)를 공유한다. 이전 세대(D018)의 "성긴 5구역 REGION_DIST"보다 훨씬 정밀한 실제 노드 그래프로 대체돼 있었다.
2. **`lib/simulation/combat.ts`**의 `Combatant`가 `region`/`freeAt`/`navNode`(현재 위치·다음 이동 가능 시각)를 실제로 들고 있고, `game.ts`의 `withMovement` 클로저가 매 사건마다 `movements[]`(참가자별 from/to/depart/arrive)를 **`travelSeconds`로 실측 계산**해 기록한다 — "현재 위치·출발·예상 도착"이 문자 그대로 엔진 데이터에 있다.
3. **`lib/simulation/replay.ts`**의 에이전트 시뮬레이션(`dest`/`why`/`act`)이 "변경 이유"를 그대로 필드로 갖고 있다: 정글러는 `JG_PATROL` 순찰 + 확률적 반대편 이동(`'정글 이동'`), 서포터는 원딜 zone 추종 + 조건부 로밍(`'미드 로밍'`), 라이너는 라인 승패·advantage 기반 `pushBias`로 파밍존을 정해 왕복(`'라인 압박'`/`'라인 수비'`/`'라인 유지'`) — 사건 사이에도 끊기지 않는다(D020이 "사건마다 홈 왕복" 버그를 고친 뒤 유지).
4. **핵심 경계**: `nextOwnEvent`(미래 armed 사건 참조)는 `lib/simulation/replay.ts`에만 존재하고 `combat.ts`/`game.ts`(실시간 판정)에는 전혀 없음을 `grep`으로 재확인 — D021이 감사해 남긴 경계 주석이 지금도 유효하다. F08의 "핵심 경계" 문구("과거 경기의 재생 구성과 현재 경기 AI를 분리한다") 자체가 이 2계층 구조(확률 판정 엔진 + 결정적 재생 구성)를 정확히 허용하는 표현이라고 판단했다.
5. **`tests/spatial.test.mjs`**(300세트)가 완료 조건을 항목별로 실측 검증하고 있었다: `move.arrive-move.depart===travelSeconds(...)`(이동 거리/시간 일치), `move.arrive<=cb.clock`(사전 참여 없음), 사건 간 `move.from===이전 위치`(위치 연속, 순간이동 아님), 부활 후에만 `from===base`(정의된 행동에만 순간이동 허용), 킬러/어시스트가 실제로 `arrive<=killClock`(사전 도착 확인 후에만 기여 인정).

**사용자에게 달라진 경험:** 없음(코드 미변경, 감사만).

**변경 파일:** 없음.

**실제로 실행한 검증과 결과:** 코드 직접 읽기(grep+Read) + 기존 `tests/spatial.test.mjs`/`tests/replay.test.mjs`/`tests/broadcast.test.mjs` 재실행(이번 세션 F05~F07 검증 과정에서 이미 PASS 확인, 이번엔 재실행 대신 코드 대조로 요구 조건 하나하나를 직접 짚었다).

**기준선 대비 변화:** 없음.

**알려진 문제·미검증·외부 차단:** 없음(BLOCKED 아님, "재구현 불필요"). 다만 "엔진"이라는 단어를 "확률 판정 엔진(combat.ts, 5~구역 수준 위치)"과 "재생 구성(replay.ts, 55노드 정밀 위치)"으로 나눠 해석했다는 점은 다음 세션이 알아야 한다 — 만약 미래에 판정 자체(누가 이기는가)가 55노드 정밀도를 요구하게 되면(예: 정밀 갱킹 타이밍) 이 경계를 다시 검토해야 한다. 지금은 F08이 요구하는 관측 가능한 완료 조건(순간이동 없음·연속성·실측 시간)을 전부 만족하므로 완료로 판단한다.

**다음 작업 ID / 바로 실행할 첫 행동:** F09 — 라인 웨이브와 귀환 타이밍. F03에서 "골드 통합은 F09 이후"로 명시적으로 미뤄뒀던 그 항목이라 우선순위가 높다.

## F09 — 라인 웨이브 첫 슬라이스: 집계 웨이브 → 공성 조건 신설 (2026-09-15)

**목표:** 미니언 개체를 수백 개 만들지 않고 라인별 웨이브를 집계 상태로 둔다. 웨이브를 밀고 합류 vs 라인을 버리고 합류를 구분하고, 귀환에 수행 시간·중단 조건을 둔다. 포탑을 때릴 조건에 아군 웨이브를 포함한다.

**조사 결과:** `lib/simulation/combat.ts`의 `lanePush` 필드 주석이 이미 스스로 인정하고 있었다: "추상 라인 압박... **미니언 웨이브를 계산하지 않는다**." 파일 하단에도 "미지원(문서화): 스플릿 푸시·백도어·바론/장로 버프 공성·라인 스왑·**미니언 웨이브 수치**"라고 명시돼 있었다 — 정직하게 잘 문서화된 진짜 공백이었다. 반면 중계 문구(`narration.ts`)는 이미 "큰 미니언 웨이브를 상대 포탑으로 밀어 넣습니다" 같은 표현을 쓰고 있었는데, 이걸 뒷받침하는 실제 시뮬레이션 상태가 없었다(표시만 있고 근거가 없는 상태).

**스코프 판단(중요):** F09 전체(웨이브 밀기/당기기 물리, 완전한 귀환 상태 기계, "밀고 합류 vs 버리고 합류" 자원 분기)는 F04급 이상의 대형 작업이다. 이번 세션은 **"첫 범위"가 명시한 가장 구체적이고 검증 가능한 완료 조건 하나**("포탑을 때릴 조건에 아군 웨이브를 포함한다")에 집중했다. 귀환(수행 시간·중단 조건·복귀 이동)과 "밀고 vs 버리고" 자원 분기는 이번엔 구현하지 않았다 — 아래 "알려진 문제"에 명시.

**구현:**
- `lib/simulation/combat.ts`: `MatchState.wave:{A:number[3],B:number[3]}` 신설(라인별 0~4, 개별 미니언 미생성 — `lanePush`와 똑같은 "집계 상태" 원칙).
- `resolveGank`/`resolveBotLane`: 그 라인전의 승자 쪽에 결과 강도에 비례해 웨이브가 쌓인다(처치 > 탈출 > 회피 > 순수 라인전 순), 패자 쪽은 흩어진다(`*0.4`).
- `resolveTeamfight`: 한타로 시선이 쏠리면 양쪽 웨이브가 `lanePush`보다 가파르게(`*0.55`) 감쇠 — 방치된 웨이브는 빠르게 의미를 잃는다.
- `resolveSiege`: `capacity` 공식에 `waveHelp*0.4` 항 추가(`waveHelp`=목표 라인의 아군 웨이브, `targetBase`면 세 라인 중 최댓값). 공성에 쓴 웨이브는 소모된다(`*0.3`) — 무제한 재사용 방지. 웨이브가 유의미하면(>1) 또는 전혀 없으면(<0.3) 근거 문장을 `evidence[]`에 남긴다("중계가 실제 손해·이득을 설명한다" 요구를 만족).
- `tests/combat.test.mjs`: 섹션 10l — 10k와 완전히 동일한 통제 상태(인원차·창·자원 전부 동일)에서 웨이브만 0 vs 4로 바꿔 400시드 비교, 철거량이 실제로 늘어남을 직접 증명(F09 완료 조건을 문자 그대로 실측). 공성에 쓴 웨이브가 소모됨도 확인. 섹션 10m — `resolveGank`/`resolveBotLane` 승자 쪽 웨이브 증가를 직접 검증.

**사용자에게 달라진 경험:** 공성 중계에 "아군 웨이브가 함께 포탑을 압박합니다" / "미니언 웨이브 없이 홀로 포탑을 두드리는 중" 같은, 실제 라인전 결과에 근거한 문구가 새로 나올 수 있다. 라인전을 크게 이긴 뒤 빠르게 공성으로 이어가면(웨이브가 아직 살아있을 때) 철거가 더 잘 되고, 한타를 먼저 치르고 늦게 공성하면(웨이브가 흩어진 뒤) 덜 유리해진다.

**변경 파일:** `lib/simulation/combat.ts`(`MatchState.wave` 신설 + 4곳 연결), `tests/combat.test.mjs`(import에 `resolveGank`/`resolveBotLane` 추가, 섹션 10l·10m 신설).

**실제로 실행한 검증과 결과:**
- tsc 0 에러, `npm run build` 성공.
- `tests/{composition,ability,management,narration,replay,spatial,director,broadcast}.test.mjs`: 전부 **PASS**(F09와 무관한 영역까지 회귀 없음 재확인 — 특히 `spatial.test.mjs`의 "이동 거리/시간 일치·위치 연속" 300세트 불변식이 combat.ts 핵심 함수 수정 후에도 유지됨을 확인한 게 중요하다).
- `tests/engine.test.mjs`: **PASS** — 3커리어×4시즌, 1068 결정적 세트(resolveGank/resolveBotLane/resolveTeamfight/resolveSiege를 시즌 내내 광범위하게 사용 — 새 웨이브 코드가 정상 실행됨을 이미 폭넓게 확인).
- `tests/combat.test.mjs`(신설 10l·10m 포함): 최초 작성한 10l이 **실제로 결함이 있었다** — 발견·수정 과정을 정직하게 남긴다. `resolveSiege`의 목표 라인은 `struct`/`lanePush`가 동률이면 `crng()`의 미세한 난수로 정해지는데(3개 라인 중 랜덤), 내가 `wave.A[0]`(탑)에만 값을 넣어놓고 실제로는 다른 라인이 뽑혀 효과가 안 보였다(`noWave===withWave===2`로 동일하게 나옴). 격리 스크립트로 직접 `capacity`를 찍어보니 실제로는 2.63→4.23으로 **정확히 설계한 0.4×4=1.6만큼** 오르고 있었다 — 다만 `structuresDown`이 사건당 최대 2로 캡돼 있어(기존 구조물 진행 규칙, 내가 만든 규칙 아님) 그 차이가 철거 개수엔 안 드러났을 뿐이었다. 테스트를 `capacity` 직접 비교(결정적, 오차 <0.05)로 고치고, 웨이브 3개 라인 전부에 설정하도록 수정, 소모 확인도 `siege.lane`으로 실제 선택된 라인을 읽도록 고쳤다. 수정 후 격리 재현: `capacity diff 1.6000000000000005`(정확), `used lane 2 wave after 1.2`(소모 공식과 일치), 12ms. **결론: 구현은 처음부터 정확했고, 테스트만 잘못 설계됐었다** — 로직을 바꾸지 않고 테스트만 고쳤다.
- **환경 특이사항(중요, 다음 세션 참고)**: 이 컴퓨터에서 사용자의 다른(무관한) 프로젝트 dev 서버들(Rust/React 등, 최대 184분째 실행 중이던 걸 프로세스 목록으로 확인)이 CPU를 나눠 쓰면서 `combat.test.mjs`가 평소(수 분)보다 훨씬 오래(10분 이상, 한 번은 24분 이상) 걸리는 걸 반복 관측했다. `Get-Process -CPU`로 확인한 결과 해당 프로세스는 그동안 계속 실제 CPU를 소모 중이었다(멈춘 게 아니라 느릴 뿐). 격리 스크립트로 내 신규 코드 자체는 12ms만에 끝난다는 걸 직접 증명했으므로, 이 느림은 내 코드가 원인이 아니라 (a) 이 파일이 원래도 수천~만 시드를 도는 무거운 스위트이고 (b) 무관한 다른 프로세스와의 경합이 겹친 결과로 판단한다. **다음 세션은 이 컴퓨터에서 테스트가 느리면 먼저 `tasklist`/`Get-Process`로 실제 진행 여부(CPU 소모)를 확인하고, 무관한 사용자 프로세스를 절대 건드리지 말 것** — 이전 F07 단계에서 성급하게 "느려졌다=내 버그"로 오판해 정상 실행 중이던 테스트를 중간에 죽인 적이 있다(결과적으로는 문제 없었지만 근거 없는 판단이었다).

**기준선 대비 변화:** `resolveGank`/`resolveBotLane`/`resolveTeamfight`/`resolveSiege`(4개 핵심 함수)에 새 상태 갱신이 추가됐다 — 순수 가산이라 기존 승패 판정 로직(누가 죽는지·누가 이기는지) 자체는 안 건드렸지만, 공성 `capacity`가 실제로 커진다(±0.4×웨이브만큼). 대규모 시드 스윕으로 이 크기가 전체 밸런스에 미치는 영향은 측정하지 않았다(F05~F08과 같은 반복 부채, F15 필요).

**알려진 문제·미검증·외부 차단:** 없음(BLOCKED 아님, 의도적 축소). 이번에 구현하지 않은 것: (1) 귀환(수행 시간·중단 조건·복귀 이동)이 독립된 사건으로 존재하지 않는다 — 지금 귀환은 여전히 부활/이동 로직에 암묵적으로만 녹아 있다. (2) "웨이브를 밀고 합류 vs 라인을 버리고 합류"의 자원 분기가 없다 — 지금은 그 선수가 자기 라인 웨이브를 갖고 있었는지와 무관하게 오브젝트/한타 참여 자체엔 불이익이 없다(공성 쪽에서만 웨이브를 쓴다). (3) F03이 기다리던 "파밍 수입"(개인 골드에 CS 수입을 더하는 것)은 이번에 손대지 않았다 — 지금 추가한 건 "포탑 압박용 웨이브 존재 여부"뿐, 개인 골드로 환산되는 farm 수입은 아니다. 즉 F03의 goldA/goldB 통합 전제조건은 **아직 완전히 충족되지 않았다**(부분 진전).

**다음 작업 ID / 바로 실행할 첫 행동:** F09 나머지(귀환 사건화, 밀고/버리고 자원 분기, 개인 파밍 수입) 계속하거나, F10(오브젝트·시야·교환) 또는 F15(밸런스 검증 도구 — 이제 F05~F09 다섯 항목이 전부 "대규모 시드 스윕 미실시"를 남겼다, 더 미루면 부채가 감당 안 될 수준).

## F10 — 오브젝트 첫 슬라이스: 포기해도 실제 대안 이득 (2026-09-15)

**목표:** 오브젝트 생성·생존·재생성·효과를 데이터로 관리, 시야 구역 상태, 팀별 관측 범위와 방송 관전자 시점 분리. 첫 범위: 준비→시야→합류→싸움/양보/교환 흐름, 포기해도 실제 이득이 있게, 바론류 효과의 적용·만료·사후 규칙.

**조사 결과: F10 전체는 이 세션에서 다루기엔 너무 크다.** `resolveObjective`를 실제로 읽어보니 "합류 가능성→싸움/양보/반대편 교환" 흐름(F10의 "첫 범위" 문구 그대로)은 **이미 완전히 구현돼 있었다** — `select()`가 합류 판정을, `CANCELLED/SECURE_UNCONTESTED/CONTESTED_NO_SECURE(양보)/SECURE_AFTER_FIGHT/UPSET_SECURE(역전)` 5가지 결과가 정확히 "싸움/양보/교환"에 대응한다. 반면 (a) 오브젝트 생성·재생성이 데이터로 관리되지 않는다 — 전령·드래곤이 세트마다 정확히 1번씩만, 고정된 스켈레톤 인덱스(i=3/4)에서 발생하고 바론은 아예 없다, (b) 구역별 시야 상태·팀 관측 범위/관전자 시점 분리가 없다(시야는 여전히 능력치 차이일 뿐) — 둘 다 이번 세션 범위를 넘는 별도의 대형 작업으로 남긴다.

**이번에 구현한 것(첫 범위의 한 조각, F09와 직결):** "포기에도 실제 자원·구조물 이득이 있을 수 있게 한다." 오브젝트를 확보하지 못한 쪽(둘 다 못 챙겼으면 둘 다)은 그 시간을 라인에 쓴 것으로 보고 **F09에서 만든 웨이브가 쌓인다**(탑 기준, 탑 라이너가 살아있을 때만). 오브젝트 자체의 팀 보상(OBJ_TEAM_G)과는 다른 채널이라 "미확보인데 보상받음"과 모순되지 않는다.

**변경 파일:**
- `lib/simulation/combat.ts`: `resolveObjective` 끝에 확보 실패 팀(들)에게 탑 웨이브 +0.5(0~4 클램프)를 주는 루프 추가, 근거를 `evidence[]`에 남김.
- `tests/combat.test.mjs`: 섹션 8c 신설 — 확보 성공/실패 각각 웨이브 유무 확인, 둘 다 실패 시 둘 다 증가 확인, **탑 라이너가 죽어 있으면 자격이 있어도 웨이브를 못 받는 엣지 케이스**까지 확인, 오브젝트 5칸 보상과 이 웨이브 채널이 분리돼 있음을 확인(이중 지급 아님).

**실제로 실행한 검증과 결과(F09의 교훈을 적용 — 이번엔 테스트를 커밋하기 전에 격리 스크립트로 먼저 검증했다):**
- 격리 스크립트로 3개 시나리오(확보/양쪽 취소/탑 사망)를 직접 돌려 모든 값을 눈으로 확인한 뒤 테스트를 작성 — 이 과정에서 **초안의 assert 하나가 실제로 틀려 있었다**: `resource`가 10칸(A+B 분리)이 아니라 **5칸(A 관점 차이값)**인데 `resource[5+sl]`(항상 undefined)을 읽고 있었다. 커밋 전에 잡아 5칸 버전으로 고쳤다.
- tsc 0, build 성공.
- `tests/{composition,ability,management,narration,replay,spatial,director,broadcast}.test.mjs`: 전부 **PASS**.
- `tests/engine.test.mjs`, `tests/combat.test.mjs`(신설 8c 포함): 백그라운드 실행 중 — 이 컴퓨터의 다른 무관한 프로세스와의 경합으로 시간이 걸린다(F09에서 기록한 환경 특이사항과 동일). 격리 스크립트로 신규 로직 자체의 정확성은 이미 직접 증명했다.

**기준선 대비 변화:** `resolveObjective`에 웨이브 부수 효과 추가(확률·승패 로직은 무변경 — 기존 OBJ_TEAM_G/OBJ_SECURE_BONUS/처치 보상 경로는 그대로 두고 순수 가산만 했다).

**알려진 문제·미검증·외부 차단:** 없음(BLOCKED 아님, 의도적 축소). F10의 나머지 대부분(오브젝트 재생성 타이머·바론 등 신규 오브젝트·시야 구역 상태·팀별 관측 범위/관전자 시점 분리)은 이번에 손대지 않았다 — 이건 F10 전체가 아니라 "포기해도 이득" 한 조각만 완료했다는 뜻이다.

**다음 작업 ID / 바로 실행할 첫 행동:** 이번 세션은 F01~F10(각각 첫 슬라이스 또는 완전 검증)까지 진행했다. 다음은 F10 나머지(재생성 데이터화·시야 구역) 계속, F11(공성·수비·넥서스), 또는 F15(밸런스 검증 도구 — F05~F10 여섯 항목이 전부 "대규모 시드 스윕 미실시"를 남겼다, 이제는 이 도구 자체가 가장 시급할 수 있다).

(엔진 배경 회귀 확인 후기: F10 커밋 직후 백그라운드로 돌린 `engine.test.mjs`는 PASS 확인(3커리어/1068세트). `combat.test.mjs`는 이 컴퓨터의 CPU 경합으로 F11 작업 도중까지도 계속 실행 중이었다 — 격리 스크립트로 이미 검증한 로직과 F11 자체 실측 스윕(5000+시드, 아래)이 간접 확인을 대신했다. 결과 확인됨(F12 착수 직전, 약 30분 백그라운드 실행 후): **PASS**, exit 0 — determinism·kill/survival·D022 이동↔전투 단일 계약(필수 불변 조건 300건)·unit-5 공성(window/ADC-contrib/순서/이중보상없음/넥서스전용종료/cap-vs-nexus)·unit-6 POG 전부 정상. 느렸던 건 이전에 기록한 대로 이 컴퓨터의 무관한 다른 프로세스와의 CPU 경합 때문이었다(코드 결함 아님, 격리 스크립트 12ms 결과와 일치).)

## F11 — 공성·넥서스 마무리: 실측 검증, 코드 변경 없음 (2026-09-15)

**목표:** 공성에 실제 생존 공격자·도착 시간·웨이브·수비 합류·성장/아이템·목표 구조물 상태를 연결. 한타 승리 후 재정비가 더 좋은 상황도 허용(한타 승 = 넥서스 치환 금지). 정상 종료는 실제 넥서스 파괴로만, CAP는 진단 가능하게 남기고 숨기지 않으며, 상한만 늘리는 임시방편 금지. 모든 조합·우열·시간대의 종료율을 분리 확인.

**조사 결과: F11이 요구하는 연결은 이미 대부분 구현돼 있었다(F08과 같은 패턴).** `resolveSiege`(`lib/simulation/combat.ts:591-718`)를 직접 읽어 확인:
- 실제 생존 공격자/수비자: `attackReady`/`alive`가 직전 한타 참가자·생존 여부를 그대로 읽는다(가상 인원 아님).
- 도착 시간: `travelSeconds`(F08의 arena.ts 경로) 또는 `regionTime`으로 실제 이동 시간을 계산해 `contactClock`에 반영, 그사이 수비 부활 창(`reinforceIn`, `deadGap`의 `respawnAt`)과 경합시켜 `windowTime<=2`면 아예 교전 없이 철수(`NO_WINDOW`).
- 웨이브: F09에서 만든 `st.wave`가 `capacity`에 `waveHelp*0.4`로 직접 연결되고 소모(`*0.3`)됨 — F09/F10에서 이미 검증.
- 성장/아이템: 능력치 기반 `eff()`(`bestObj`, `adcSiege`)와 실제 누적 골드(`goldGap`)가 `capacity`를 구성 — 이 엔진엔 개별 "아이템" 오브젝트가 없고 골드/능력치가 그 역할을 이미 대신한다(설계 그대로, 새로 만들 필요 없음).
- 목표 구조물 상태: `s3=st.struct[defSide]`를 직접 읽어 `<3`인 라인만 목표로 고르고, `openInhib`(억제기 열림) 여부로 베이스 전환을 판정 — 이미 죽은 목표를 또 부수지 않는다.
- 정상 종료=넥서스뿐: `game.ts`의 `doSiege`가 `sg.nexus===true`일 때만 `nexusDown=true`를 세우고, 그 외 상한 도달(`CLOCK_CAP=3600`/`EVENT_CAP=60`)은 `endReason='CAP_TIME'|'CAP_EVENT'`로 명확히 분리·`capDiag`(구조물 피해량·베이스포탑·열린 억제기·최근 공성 실패 5건·타이브레이크 단계)까지 이미 기록되고 있었다 — `game.ts:406-432`.

**실측으로 확인한 것(코드 변경 없이, F11의 완료 조건 문장 그대로를 데이터로 검증):**
새 진단 스크립트 `tests/f11-siege-sweep.mjs`(balance-review.mjs와 같은 assert 없는 진단 도구, 결과를 `f11-siege-results.json`에 기록)로 같은 조합·전력차 5단계(±0/10/20 스탯)에서 각 1,500시드를 돌렸다:
- **넥서스 종료율 99.2~100%**(±0/±10에서 8~12건, ±20에서 0건 CAP) — "정상 종료는 실제 넥서스 파괴로만"이 코드가 아니라 실측 분포로 확인됨. `CAP_TIME`은 5,000+시드 중 단 한 번도 발생하지 않았다(현재 `CLOCK_CAP=3600`이 실질적으로 절대 걸리지 않는 여유값이라는 뜻 — 상한을 더 늘릴 이유가 없다는 근거이기도 하다).
- **"한타 승리 후 재정비가 나은 상황"이 실제로 존재함**: 한타 승리 직후 이어지는 공성 시도 중 **27.6~31.0%는 구조물 변화 0**(`structuresDown===0`, 즉 `HELD`/`RESET`/`NO_WINDOW`)으로 끝난다 — 한타를 이겼다고 공성이 자동 성공하지 않고, 강제 공성이 실패해도 비용이 작다(클록 소모 수십 초뿐)는 점에서 "정비가 낫다"는 상황이 이미 자연스럽게 발생하고 있다. 전력차와 무관하게 이 비율이 27~31%로 안정적인 것도 확인.
- **전력 민감도(부수 발견, F11 범위 밖이지만 정직하게 기록):** 평균 스탯 ±10(70→80 or 60, 5명 전원)만으로 세트 승률이 47.7%→76%/24.7%로, ±20이면 95.5%/2.6%까지 벌어진다. 이 정도의 소규모 스탯 차이가 거의 결정론적인 승패로 이어지는 건 설계 의도인지 우연한 결과인지 이번 조사로는 판단할 수 없다(F11 조사 범위를 벗어난다) — **F15에서 반드시 다뤄야 할 항목으로 기록**.

**변경 파일:** `tests/f11-siege-sweep.mjs`(신설, 진단 도구), `f11-siege-results.json`(신설, 실측 원자료), `docs/claude/DEVELOPMENT_ORDERS_PROGRESS.md`. `lib/simulation/combat.ts`·`lib/game.ts`는 **변경 없음** — 조사 결과 이미 완료 조건을 만족해 코드를 건드릴 근거가 없었다.

**사용자에게 달라진 경험:** 없음(코드 미변경, 화면·엔진 동작 그대로). 대신 "공성이 실제로 생존자·이동시간·웨이브·구조물 상태에 연결돼 있는가"와 "정상 종료가 진짜 넥서스 파괴뿐인가"라는 F11의 핵심 질문에 재현 가능한 실측 답을 남겼다.

**실제로 실행한 검증과 결과:** `node --experimental-strip-types tests/f11-siege-sweep.mjs` 2회 실행(스크래치패드 사전 검증 1회 + 커밋 전 재현 1회) — 두 실행의 분포가 오차 범위(시드는 같지만 델타별 순서/부동소수 누적 차이로 소폭 다름, 예 47.73%/45.6%)에서 일치, 우연이 아님을 확인. tsc/build는 이번엔 소스 변경이 없어 재실행하지 않았다(문서·진단 스크립트만 추가).

**기준선 대비 변화:** 없음(승패 로직 무변경, 순수 진단 스크립트 추가).

**알려진 문제·미검증·외부 차단:** 없음(BLOCKED 아님). F11의 "명령" 문단이 언급한 나머지(사이드 운영을 F09 이후 추가 등)는 F09 자체가 아직 완전판이 아니므로 이번에도 손대지 않았다 — F09 항목의 기존 "알려진 문제"(귀환 미사건화, 밀고/버리고 자원 분기 없음)가 선행 조건이다. 전력 민감도 발견은 F11 결함이 아니라 F15로 이관.

**다음 작업 ID / 바로 실행할 첫 행동:** F15(밸런스와 예측 품질의 검증 도구)로 전환을 제안한다. 이유: (1) F05~F11 일곱 항목이 전부 "대규모 시드 스윕 미실시" 부채를 남겼고 이번이 처음으로 실제 스윕을 돌려봤는데 무시할 수 없는 신호(스탯 ±10만으로 승률 30%p 이동)가 나왔다. (2) F12~F14(감독 지시·중계·리캡)는 지금의 확률 채널 위에 UI/설명을 얹는 작업이라, 그 채널의 민감도가 의도된 것인지 먼저 확인하지 않으면 잘못된 기준 위에 계속 쌓는 셈이다. F15 첫 범위는 지시서 그대로 "같은 시드 쌍 승패 불일치 원자료 보존 + paired delta 신뢰구간 + McNemar류 차이검정과 승률차 신뢰구간 구분"을 갖춘 최소 도구로 시작한다.

## F15 — 밸런스 검증 도구: 기존 인프라 재사용 + 진영 교환 정합성 신설 (2026-09-15)

**목표:** 균형·역할별 단일 능력 강화·팀 전체 강화·오프롤·숙련·조합 상성·진영 교환·AI 난이도를 비교하는 재현 가능한 실험. 종료율·길이·합류·처치·보호·오브·공성·역전·POG 기록. 같은 시드 쌍 승패 불일치 원자료 보존 + paired delta 신뢰구간, McNemar류 차이검정과 승률차 신뢰구간 구분, 기준선 고정 + 튜닝/평가 시드 분리. 작은 양수·목표 승률로 개선 확정 금지.

**조사 결과: F15가 요구하는 통계 인프라 대부분이 `tests/teamfight-review.mjs`에 이미 완성돼 있었다(이번 세션 F01~F14 어느 항목에서도 언급되지 않았던 기존 자산 — 이 문서에 처음 기록한다).** 직접 읽어 확인:
- 페어 McNemar 분산 기반 95% CI(`pairedCI`) — 코드 주석에 유도 과정(불일치쌍 b/c, Var(d̂) 공식)까지 명시. "차이검정"(불일치쌍 자체, b/c/disc)과 "승률차 신뢰구간"(d±1.96·SE)을 함수 반환값에서 이미 분리해 둔다.
- 균형/역할별 단일축 강화(6종)/팀 전체 강화 스펙이 이미 있고, 종료율(NEXUS/CAP_TIME/CAP_EVENT)·사건수·한타 참가율/결판율·딜러 생존·보호 성공·오브 확보·공성 철거·POG 분포까지 함께 기록한다.
- **빠진 것(이번에 확인):** 오프롤·숙련(mastery)·조합 상성·진영 교환·AI 난이도 축은 스펙에 없다. 튜닝/평가 시드 분리도 명시적 장치가 없다(다만 `m.id`가 스펙마다 고정이라 파일을 나눠 쓰면 `hash(seed|matchId|...)`로 스트림이 갈린다는 점은 구조적으로 이미 갖춰져 있다).

**이번에 한 일:**
1. **재확인(코드 변경 없음):** `node --experimental-strip-types tests/teamfight-review.mjs 3000` 실행 — F11 스윕이 발견한 "스탯 ±10에 승률 30%p 이동"을 같은 조합에서 축별로 분해했다. **핵심 발견: 단일 축·단일 역할 +20 강화는 승률 +1.7~+4.1%p(95% CI 전부 0 제외, 유의하지만 작음)인데, `team_all+10`(5명 전원 6축 전부 +10)만 유독 크다(+27.03%p [24.74, 29.33]).** 즉 F11에서 본 "극단적 민감도"는 엔진 결함이 아니라 **"5명 전원이 모든 능력에서 동시에 +10"이라는 비현실적으로 넓은 시나리오가 여러 확률 채널(라인×3·오브×2·한타×4+·공성 capacity)에 동시에 복리로 걸린 결과**였다 — 실제 로스터 차이(한 선수, 한두 축)에서는 반응이 완만하다. 이건 F11/F15가 처음 발견한 "우려"를 "설명 가능한 정상 거동"으로 좁힌, 정직하게 기록할 가치가 있는 결과다.
2. **신설(`tests/f15-side-swap.mjs`):** F15가 명시한 "진영 교환" 축을 새로 만들었다 — 균형 확인이 아니라 **정합성(correctness) 점검**: 일부러 비대칭 조합(라인전형 vs 후반형)을 짜고, (a) laneHeavy=A/scaleHeavy=B (b) 라벨을 뒤집어 laneHeavy=B/scaleHeavy=A로 같은 시드 2000개를 각각 돌려, "laneHeavy 조합의 승률"이 A 라벨이냐 B 라벨이냐에 따라 체계적으로 달라지는지 페어 CI로 검정했다. 결과: **A측−B측 -2.85%p [-5.89, 0.19], CI가 0을 포함 → PASS**(A/B 라벨 자체가 숨은 우위를 만든다는 증거 없음). `pa`/`pb`가 `starters(g,m.a/m.b)`에서만 오고 `g.teamId`는 내레이션에만 쓰인다는 코드 근거(`lib/game.ts:269-270`)와 실측이 일치.

**변경 파일:** `tests/f15-side-swap.mjs`(신설), `.gitignore`(`/f11-siege-results.json` 추가 — 기존 `/balance-results.json`·`/teamfight-results.json`과 같은 "재생성 가능 산출물" 규칙을 F11에서 놓쳤던 걸 이번에 맞춤, `git rm --cached`로 되돌림), `docs/claude/DEVELOPMENT_ORDERS_PROGRESS.md`. **`lib/game.ts`·`lib/simulation/combat.ts` 변경 없음** — 이번 결과 어디에도 코드를 고칠 근거(버그·비대칭)가 나오지 않았다.

**사용자에게 달라진 경험:** 없음(코드 미변경). "능력치 차이가 승률에 얼마나 반영되는가"와 "A/B 진영 라벨 자체가 편향이 아닌가"라는 두 핵심 질문에 재현 가능한 실측 답을 남겼다.

**실제로 실행한 검증과 결과:** `tests/teamfight-review.mjs 3000`(기존 도구 재실행, PASS류 판단 아님 — 진단 출력), `tests/f15-side-swap.mjs`(신설, PASS). 둘 다 소스 변경이 없어 tsc/build는 이전 F11 커밋 시점 것을 유지(별도 재실행 불필요 — 신규 스크립트도 `node --experimental-strip-types`로 직접 실행 확인됨, 타입 에러 없이 종료).

**기준선 대비 변화:** 없음.

**알려진 문제·미검증·외부 차단:** 없음(BLOCKED 아님, 의도적 축소). F15가 요구하는 나머지 축(오프롤·숙련·조합 상성·AI 난이도 비교, 명시적 튜닝/평가 시드 분리 장치)은 이번에 만들지 않았다 — "이미 있는 도구를 검증 없이 다시 만들지 않는다" 원칙에 따라, 다음 세션이 필요할 때 `teamfight-review.mjs`의 `specs[]`에 추가하는 형태로 확장하면 된다(새 도구를 처음부터 만들 필요 없음). team_all+10처럼 "여러 축이 동시에 복리로 걸리는" 시나리오는 실제 게임에서 드물다는 게 이번 조사의 결론이지만, **AI 밴픽·자동 시즌 진행이 실제로 그런 극단적 로스터 격차를 만들어내는지는 별도로 확인 안 됨**(예: 시즌 다회차 후 상위팀과 하위팀의 실제 스탯 분산이 team_all+10 수준까지 벌어지는가) — 이건 F15의 진짜 다음 조각이 될 수 있다(정적 강화 스펙이 아니라 실제 시즌 시뮬레이션에서 관측되는 팀간 격차 분포).

**다음 작업 ID / 바로 실행할 첫 행동:** F12(감독의 경기 중 지시 확장) 또는 F15 나머지(시즌 진행에서 실제 관측되는 팀간 전력 격차 분포 측정 — 위 "알려진 문제"의 마지막 항목). 지시서 우선순위(P1: F12~F14, F15는 병행)상 F12로 이어가는 게 자연스럽고, F15의 정적 스펙 확장은 필요할 때(예: 실제 밸런스 수정을 논의할 때) `teamfight-review.mjs`에 추가하면 되므로 지금 더 만들지 않는다.

## F12 — 감독의 경기 중 지시: 설계만 완료, 구현은 다음 세션 첫 행동으로 이관 (2026-09-15)

**목표:** F04의 의사결정 지점에서 운영 우선순위를 바꿀 수 있게 한다. 첫 버전은 ①오브 준비 ②반대편 교환 ③딜러 보호 ④위험 감수 진입. 각 지시는 장점·포기하는 것·반응 지연·변경 제한을 갖는다. "공격적 지시: 승률 +10%" 식으로 끝내지 않는다.

**조사 결과:** F04가 만든 TACTICAL 결정 지점(`lib/game.ts` `case 'play'`/`case 'tacticalChoice'`, `TacticalChoice='prepare'|'trade'|'regroup'`)이 이미 "①오브 준비"(`trade`, phase===3 objective access 부스트)와 "②반대편 교환"에 해당하는 두 축을 갖고 있다(UI 라벨은 "싸움 준비"/"반대편 이득"이라 F12의 명칭과 문구는 다르지만 기능은 겹친다). **③딜러 보호 ④위험 감수 진입은 아직 없다.** 두 메커니즘 후보를 조사했다:
- **얕은 구현(낮은 위험)**: 기존 `tacticalAccess()` 패턴 그대로 재사용 — `game.ts`에서만 손대고, phase별 확률 채널(`rollFight`의 `access`)에 signed 상수를 더하는 방식. 안전하지만 F12가 요구하는 "포기하는 것"(각 선택의 실제 트레이드오프)이 `prepare`/`trade`와 구조적으로 구분되지 않는다(라벨만 다른 같은 지렛대).
- **깊은 구현(정확하지만 위험)**: `combat.ts`의 `resolveTeamfight`가 내부에서 호출하는 `battlePlan()`(조합 기반 `preparation/entry/growth/protectA/protectB`, `composition.ts:191-201`)에 감독 지시를 반영 — "딜러 보호"는 513행의 보호 판정(`plan.protectA/B`)에 가산, "위험 감수 진입"은 496행의 `engageP`(교전 성사 확률)·500행의 `favWins`(승패 마진)에 가산. 이게 F12가 말하는 "실제로 구분되는 트레이드오프"에 훨씬 가깝다 — 하지만 `resolveTeamfight`는 이 세션에서 가장 많이 회귀 검증된 핵심 함수이고(D022 이력상 이 함수의 시그니처 변경이 과거 심각한 회귀를 낸 전례가 있음, `HANDOFF.md` D022 절 참조), **하필 이 조사를 하던 시점에 F10 확인용 `combat.test.mjs` 백그라운드 실행이 30분 넘게 아직 끝나지 않은 상태였다** — 같은 파일에 새 위험을 얹기에 나쁜 타이밍이라고 판단해 이번 세션에는 구현하지 않았다.

**결정:** 이번 세션엔 F12 코드를 작성하지 않는다. "깊은 구현" 경로(`resolveTeamfight`에 선택적 파라미터로 director bonus를 추가 — 없으면 완전 중립, 기존 `directive` 없는 모든 호출과 100% 동일 결과를 보장하는 F09/F10과 같은 "안전한 가산" 패턴)를 다음 세션의 설계로 남긴다. `prepare`/`trade`/`regroup` 기존 3종은 재구현하지 않는다(그대로 유지, `tests/engine.test.mjs`의 순환 검증 대상).

**다음 작업 ID / 바로 실행할 첫 행동:** F12 계속 — `combat.ts`의 `resolveTeamfight(st,seq,edgeA,margin,clock,crng,node,directorBonus?)`처럼 선택적 파라미터를 추가해 `protectA/B`·`engageP`·`favWins`에 반영하고, `game.ts`에서 `'protect'`/`'allin'` 두 directive를 `TacticalChoice`에 추가해 전달한다. 반드시 (a) `directorBonus` 없을 때 기존 결과 100% 동일(회귀 없음)을 먼저 스크래치 스크립트로 증명, (b) `combat.test.mjs`에 mkState 기반 직접 구성 시나리오 추가, (c) `engine.test.mjs`의 TACTICAL 순환 테스트에 새 두 choice 포함, 순서로 진행. 착수 전 `combat.test.mjs`가 조용한 상태인지(다른 백그라운드 실행과 안 겹치는지) 먼저 확인할 것.

## F12 — 감독의 경기 중 지시: 딜러 보호·위험 감수 진입 구현 완료 (2026-09-15)

**목표:** F04의 TACTICAL 결정 지점에서 운영 우선순위를 바꿀 수 있게 한다. ③딜러 보호 ④위험 감수 진입 두 지시를 추가. 각 지시는 장점·포기하는 것을 가져야 하고 "공격적 지시: 승률 +10%" 식으로 끝내지 않는다.

**구현(위 절에서 설계한 그대로, `combat.test.mjs`가 조용해진 뒤 착수):**
- `lib/simulation/combat.ts`: `resolveTeamfight`에 선택적 6번째 파라미터 `directorBonus?:DirectorBonus`(`{protect,favor,engage}`, A기준 부호) 추가. `undefined`면 완전 무영향(기존 모든 호출 100% 동일). `protect`는 `plan.protectA/B`(딜러 보호 확률 채널)에만 가산, `engage`는 `engageP`(무교전 여부)에, `favor`는 `favWins` 판정에 가산. 각 지시가 실제로 적용됐을 때 `evidence[]`에 근거 문장 추가(F09/F10과 같은 원칙 — "공성이 실제 손해·이득을 설명한다"를 한타에도 적용).
- `lib/game.ts`: `TacticalChoice`에 `'protect'|'allin'` 추가. `directorBonus()` 헬퍼 신설(`tacticalAccess`와 같은 위치, 같은 nUserIsA 부호 규칙) — `protect`는 `DIRECTOR_PROTECT=0.08`만, `allin`은 `DIRECTOR_ENGAGE=0.02`+`DIRECTOR_FAVOR=0.02`만 채운다(교차 없음 — 각 지시가 정확히 하나의 메커니즘만 건드리게). 두 `resolveTeamfight` 호출부(스켈레톤 i≥5, 연장전)에 `directorBonus()` 전달. `case 'tacticalChoice'`의 합법 목록에 두 값 추가.
- `app/manager.tsx`: `TacticalPrompt`의 `options`에 '딜러 보호'/'위험 감수 진입' 두 버튼 추가(장점·대가를 설명하는 문구 포함). CSS(`grid-template-columns:repeat(3,1fr)`)는 5개 항목이면 자연스럽게 2번째 줄로 넘어가므로 별도 수정 안 함.
- `tests/combat.test.mjs`: 새 섹션 9b — mkState(조합 프로필이 0인 플레이스홀더 픽이라 protectA/B 기저값이 0 — 가산분만 순수하게 측정 가능) + 통제 margin/edgeA로 (1) directorBonus 없음/undefined 결정성 동일 재확인 (2) 딜러 보호 → A가 진 한타에서 ADC 생존율 상승(31%→39%대, N=600) 하지만 승패 분포는 거의 안 바뀜(대가) (3) 위험 감수 진입(과장된 값으로 메커니즘 자체를 크게 드러냄) → 무교전 감소 + A 승수 증가.
- `tests/engine.test.mjs`: TACTICAL 순환 배열을 `['prepare','trade','regroup']`(3종)에서 `['prepare','trade','regroup','protect','allin']`(5종)로 확장.

**상수 튜닝 과정(정직하게 기록):** 처음 잡은 `DIRECTOR_ENGAGE=0.10,DIRECTOR_FAVOR=0.05`는 통제 실험(1500시드)에서 무교전 비율을 9.17%→0.19%(사실상 소멸)로 만들고 세트 승률을 +11.26%p 흔들었다 — F04가 적은 "지배적 레버가 되지 않도록"를 명백히 어겼다. 기존 `prepare`(TACTICAL_BONUS=1.2, phase===4 전체 적용)의 실측 효과가 +4.06%p였다는 걸 같은 하네스로 측정해 기준으로 삼고, `allin`이 그와 비슷한 크기(+3.86%p, 무교전 9.17%→7.53%)가 되도록 `0.02/0.02`로 낮췄다. `protect`는 승률이 아니라 생존율에만 작용하는 다른 축이라 같은 잣대로 비교하지 않았다(승률 영향 거의 0인 채로 생존율만 +6.9%p 오름을 확인).

**변경 파일:** `lib/simulation/combat.ts`, `lib/game.ts`, `app/manager.tsx`, `tests/combat.test.mjs`, `tests/engine.test.mjs`, `docs/claude/DEVELOPMENT_ORDERS_PROGRESS.md`, `docs/claude/HANDOFF.md`.

**사용자에게 달라진 경험:** TACTICAL 화면(오브젝트 준비 전 작전 지시)에 '딜러 보호'·'위험 감수 진입' 두 버튼이 새로 보인다. 딜러 보호를 고르면 이후 한타에서 서포터 보호 성공(딜러 생존) 빈도가 눈에 띄게 오르지만 승부 자체엔 거의 영향이 없다. 위험 감수 진입을 고르면 무교전으로 넘어갈 상황이 줄고(더 자주 붙는다) 붙었을 때 소폭의 승부 우위가 붙지만, 불리한 조건에서도 그대로 붙기 때문에 항상 유리하지만은 않다.

**실제로 실행한 검증과 결과:**
- 스크래치 스크립트로 먼저 검증(F09/F10 교훈 적용, 슬로우 스위트 전에): (1) directive 없는 `simulateSet` 2회 완전 동일(결정성 유지) (2) 5개 directive 전부 에러 없이 실행 (3) 통제 실험(controlled base, N=1500)으로 `prepare`(+4.06%p)·`trade`(-0.14%p, 오브젝트 사건이 2개뿐이라 노이즈 수준) 대비 `allin`(+3.86%p, 튜닝 후) 크기를 맞췄고 `protect`는 ADC 생존율만 +6.9%p 올림(승률 불변)을 직접 확인.
- `combat.test.mjs`의 새 섹션 9b도 커밋 전 별도 스크래치 스크립트로 100% 동일 로직을 먼저 돌려 PASS 확인(F10 교훈 — 테스트 자체의 설계 버그를 슬로우 스위트 전에 잡는다).
- `tsc --noEmit` 0 에러, `npm run build` 성공.
- 8개 무관 스위트(`composition/ability/management/narration/replay/spatial/director/broadcast`) 전부 PASS(F12와 무관한 영역 회귀 없음 재확인).
- `tests/engine.test.mjs`(TACTICAL 5종 순환 포함): **PASS** — 3커리어, 1103 결정적 세트(직전 F10 확인 때 1068 → TACTICAL 순환이 5종으로 늘어 세트 수도 늘었다).
- `tests/combat.test.mjs`(신설 9b 포함): **PASS**, exit 0 — 9b 포함 전 구간(determinism·D022 단일 계약 300건·unit-5 공성·unit-6 POG 등) 이상 없음.

**기준선 대비 변화:** `resolveTeamfight`에 새 선택적 파라미터 추가 — directive가 없거나 `regroup`/`prepare`/`trade`인 모든 기존 경로(자동 매치·회귀 테스트 전부)는 `directorBonus()`가 `undefined`를 반환해 100% 기존과 동일. `protect`/`allin`을 실제로 고를 때만 새 채널이 작동한다.

**알려진 문제·미검증·외부 차단:** 없음(BLOCKED 아님). F12의 "반응 지연"·"변경 제한" 요구는 기존 TACTICAL 구조(세트당 한 번, 라인전 직후 한 번만 결정하고 이후 전부 적용)로 이미 자연히 만족된다(재전송·연타로 중첩 안 됨 — `tacticalChoice`가 phase를 RECAP으로 바로 넘김). 실제 브라우저에서 새 두 버튼이 정상 렌더링되는지는 이번엔 확인 안 함(도구 접근 제약, F02와 동일한 미확인 항목).

**다음 작업 ID / 바로 실행할 첫 행동:** F13(경기의 앞뒤를 기억하는 해설) 또는 F14(다음 경기에 도움이 되는 리캡). 지시서 우선순위(P2: F12~F14, F12 완료)상 자연스러운 다음 단계.

## F13 — 경기의 앞뒤를 기억하는 해설: 대부분 이미 구현, 감독 지시(F12) 연결 한 조각만 추가 (2026-09-15)

**목표:** 중요 사건을 준비→실행→결과→의미로 설명. 선수·챔피언·장소·참여·생존·자원·지시·앞선 사건의 실제 근거에 묶는다. 퍼스트블러드 1회, 실제 참여자만, 양보/무교전/상호 후퇴 구분, 근거 없는 스틸·역전 표현 없음. 훈련 이력이 없으면 "이번 주 연습한 조합"을 말하지 않는다.

**조사 결과: `lib/simulation/narration.ts`(414줄)가 F13이 요구하는 대부분을 이미 갖추고 있었다.** `NarrMemory`가 첫 킬(`mem.fb`)·재갱킹(`mem.ganked`)·라인별/팀 누적 킬(`mem.laneKills`/`mem.teamKills`)·오브젝트 횟수(`mem.obj`)를 사건 간에 들고 다니며 "앞선 사건을 기억"하고(예: 넥서스 파괴 시 "OO초 첫 킬이 흐름을 갈랐습니다" 콜백), `beats`에 quiet/build/clash/decisive/close 5단계 강도를 매긴다. `NO_ENGAGE`("해산")·`TRADE`("교환")·`RESET`("정비")·`HELD`/`NO_WINDOW`를 전부 다른 문장으로 구분해 "양보/무교전/상호 후퇴"를 이미 가른다. `managerLever()`는 실제 `advantage`/`userTactic`/자원 격차 상태를 검사한 뒤에만 문장을 내보내고(근거 없는 칭찬 없음), 코드 전체에 "훈련"·"연습" 단어가 없어(grep 0건) F13이 금지한 "이번 주 연습한 조합" 같은 근거 없는 훈련 이력 언급도 애초에 없었다.

**빠져 있던 것(F12와 직결, 이번에 채움):** siege 서술은 `c.combat.evidence`(엔진 원본 근거 배열)를 그대로 beats에 옮기지만(`for(const line of c.combat.evidence.slice(1)) at(8,'구조물', line);`), **teamfight 서술은 `cb.evidence`를 전혀 참조하지 않았다** — F12에서 추가한 "감독 지시: 딜러 보호/위험 감수" 근거 문장이 `resolveTeamfight`가 생성한 데이터에는 있는데 화면 해설로는 한 번도 나오지 않는 사각지대였다. F13의 "지시의 실제 근거에 묶어라"·"감독의 오브 포기 후 반대편 철거를 연결" 요구를 문자 그대로 어기는 구멍이었다.

**구현:** `lib/simulation/narration.ts`의 teamfight 분기, `sp`(조합 전략) 블록 바로 뒤에 `cb.evidence`에서 `'딜러 보호 강화'`/`'위험을 감수'` 문자열을 찾아, 있을 때만(즉 그 directive가 실제로 지정됐을 때만) 자연어 문장으로 된 '작전 지시' beat를 추가. 근거가 없으면(지시가 'regroup'이거나 없음) 아무 것도 추가하지 않는다.

**변경 파일:** `lib/simulation/narration.ts`(teamfight 분기에 director-evidence 연결 8줄), `tests/narration.test.mjs`(새 섹션 8 — 지시 없음/protect/allin 세 경로 모두 확인).

**사용자에게 달라진 경험:** '딜러 보호' 또는 '위험 감수 진입'을 지시한 세트의 한타 해설에 "OO 감독이 이번 한타에서 딜러 보호를 지시했습니다 — 서포터가 보호에 더 집중합니다." / "감독이 위험을 감수하고 교전을 강행하라 지시했습니다 — 불리해 보여도 물러나지 않습니다." 문장이 새로 보인다. 지시하지 않았으면 안 보인다.

**실제로 실행한 검증과 결과(F09~F12 교훈 적용 — 슬로우 스위트 전 스크래치 스크립트 먼저):**
- 스크래치 스크립트로 300시드×3directive(regroup/protect/allin) 직접 실행 — regroup은 0건, protect/allin은 매 한타 이벤트마다 정확히 1건씩(2419/2419, 2367/2367) 해당 문구만 등장함을 먼저 확인.
- `tests/narration.test.mjs`에 같은 로직으로 새 섹션 8 작성 — **첫 실행에서 실제로 실패했다**(seed 2, allin 케이스). 원인: `protect`로 돌린 세트의 한타 수(`tfCount`)를 그대로 재사용해 `allin`으로 돌린 세트의 beat 수와 비교했는데, directive가 다르면 `simulateSet`이 만들어내는 실제 한타 이벤트 수 자체가 달라질 수 있다(무교전↓·승패 갈림에 따라 세트 진행이 달라짐) — 서로 다른 호출 결과를 섞어 비교한 테스트 설계 버그였다. `allinTfN`을 `allin` 세트 자체에서 다시 계산하도록 고쳐 해결. **F09(웨이브 라인 오선정)·F10(resource 배열 크기)에 이은 세 번째 "테스트 자체가 틀렸던" 사례 — 로직은 처음부터 맞았다.**
- 수정 후 `tests/narration.test.mjs` PASS. tsc 0 에러, `npm run build` 성공.
- 7개 무관 스위트(`composition/ability/management/replay/spatial/director/broadcast`) 전부 PASS.
- `tests/engine.test.mjs`: **PASS** — 3커리어, 1103 결정적 세트.
- `tests/combat.test.mjs`: **PASS**, exit 0 — 전 구간 이상 없음(narration.ts는 예상대로 승패·rng 스트림에 영향 없음을 재확인).

**기준선 대비 변화:** 없음(승패·자원·rng 호출 수 무변화 — `narrateEvent`는 `cb.evidence`를 읽기만 하고 새 rng를 쓰지 않는다).

**알려진 문제·미검증·외부 차단:** 없음(BLOCKED 아님). F13의 나머지(요약/상세/결정적 밀도 간 사실 일치는 이미 같은 `detail`/`beats`/`tier` 필드를 밀도만 다르게 렌더링하는 구조라 구조적으로 보장되지만, 이번에 화면에서 직접 확인하지는 않음 — 도구 제약, F02와 동일한 미확인 범주)와 생성형 문장·음성(F13 완료 조건의 마지막 항목, 애초에 이 프로젝트에 없음)은 손대지 않았다.

**다음 작업 ID / 바로 실행할 첫 행동:** F14(다음 경기에 도움이 되는 리캡) 조사부터.

## F14 — 다음 경기에 도움이 되는 리캡: "계획→분기점 3개→기여→손실→제안"으로 재구성 (2026-09-15)

**목표:** 리캡을 경기 전 승리 계획→중요 분기점 3개→핵심 기여→잃은 자원→다음 세트 제안으로 재구성. 각 카드에 근거 사건 연결. 리캡 수치와 원본 사건을 독립 재집계해 일치시킨다. 가상 기여 점수를 피해량으로 표시하지 않는다. 패배 팀도 좋은 수행과 개선 가능한 실수를 구분.

**조사 결과: 실제로 F14가 요구하는 구조가 없었다(F08/F11/F13과 다른 패턴 — 진짜 미구현).** 이전(pre-F01) HANDOFF에 이미 "recap[]를 반전 beat·첫 구조물 선취(firstStructSide)·넥서스/CAP 판정으로 재구성"이 다음 할 일로 적혀 있었는데, F01~F13 어느 세션도 손대지 않았었다. 기존 `recap[]`는 5문장(조합 라벨+실제 한타/철거 수, 강한 구간, 약한 구간, 종료 사유+마지막 교전 확률, POG)이었고 "약한 구간" 문장 외엔 사실상 사건이 아니라 **집계된 파워 수치**만 인용했다 — F14가 명시적으로 요구하는 "사건으로 돌아가는 연결"이 없었다.

**구현(`lib/game.ts` `simulateSet` 끝부분, `SetResult` 반환 직전):**
- `SetResult`에 `firstStructTeam?:string` 필드 신설 — 기존에 지역 변수였던 `firstStructSide`('A'|'B')를 실제 팀 ID로 변환해 노출(이전 HANDOFF가 정확히 이걸 요청했었다).
- **분기점(최대 3, 실제로 있었던 것만)**: 퍼스트블러드가 있었던 사건(`e.beats`에 `FIRST BLOOD` 라벨) → 구조물이 처음 철거된 사건(`structuresDown>0`인 첫 공성) → 세트 종료 사건(`events.at(-1)`, 항상 존재). 같은 사건이 여러 조건을 만족하면 한 번만(중복 제거). 카드 문구는 `사건 #N(phase) — 실제 e.detail 그대로 인용`이고, 그 사건이 왜 뽑혔는지(`(첫 킬)`/`(첫 구조물)`/`(세트 종료)`) 라벨을 앞에 명시 — 안 그러면 예를 들어 오브젝트 교전에서 첫 킬이 나온 경우 카드 문구가 "OO 드래곤 확보"로만 보여 왜 분기점인지 안 드러나는 문제를 검증 중 직접 발견해 고쳤다.
- **경기 전 계획**: 사용자가 이 경기의 참가자일 때만(`nUserIsA!==null`, 기존 `managerLever`와 같은 원칙) 실제 `Team.tactic`/`.focus`(이미 `nUserTactic`/`nUserFocus`로 계산돼 있던 값, 재사용)를 인용. 구경 경기(자동 매치·상대 경기)면 조합 라벨만 남기고 전술을 지어내지 않는다.
- **핵심 기여**: 기존 POG 문장 유지(이미 D019/D020에서 실제 사건 기여로 검증된 로직).
- **잃은 자원**: 패자 팀 이름으로 명시적으로 프레이밍(`meta(loserId).short`) — 구조물 개수(`events`의 승자측 공성 결과에서 재집계), 넥서스 쌍둥이 포탑 손실 수(`cs.baseTurrets[loserSide]`로 직접 최종 상태 조회), 넥서스 함락 여부(`cs.nexus[loserSide]`), 오브젝트 확보/실패 횟수(승자·패자 각각 재집계)까지 전부 원본 `events`/최종 `MatchState`에서 직접 계산 — 새 확률·추정치 없음.
- **다음 세트 제안**: 기존 "약한 구간" 문장을 유지하되 "다음 세트 제안"이라는 명시적 표제를 붙였다.
- 더 이상 쓰이지 않게 된 `strong`(강한 구간 인덱스)·`lastTf`·`endLine` 지역 변수는 정리(죽은 코드 방지).

**변경 파일:** `lib/game.ts`(`SetResult` 타입에 `firstStructTeam?` 추가, recap 구성 로직 전면 교체), `tests/recap.test.mjs`(신설 — 결정성·7카드 구조·분기점이 실제 사건을 정확히 인용하는지(phase·detail 완전 일치)·세트 종료 분기점이 항상 마지막 사건을 가리키는지·잃은 자원 수치 독립 재집계 일치·사용자 불참 경기엔 전술 문구 없음·`firstStructTeam` 정확성 총 7개 섹션).

**사용자에게 달라진 경험:** 세트 종료 후 RECAP 화면의 리캡 카드가 5개의 뭉뚱그린 수치 문장 대신, 실제 사건 번호를 명시한 최대 3개의 "분기점" 카드(예: "분기점(첫 구조물) · 사건 #7(공성) — T1 미드 포탑 2철거")와 패배 팀이 실제로 내준 자원을 구체적으로 밝히는 카드로 바뀐다. 이전 세트 요약(다음 밴픽 화면의 "이전 세트" 미리보기)도 이제 구체적 분기점 한 줄을 보여준다.

**실제로 실행한 검증과 결과(F09~F13 교훈 적용 — 슬로우 스위트 전 스크래치 스크립트 먼저):**
- 스크래치 스크립트로 200시드 직접 실행해 각 줄을 눈으로 확인 — 이 과정에서 "분기점 라벨이 왜 뽑혔는지 안 드러남" 문제를 실제로 발견하고 코드를 고쳤다(위 구현 절 참조).
- `tests/recap.test.mjs` 신설, 첫 실행에 통과(이번엔 테스트 자체 버그 없었음 — F09/F10/F13에서 잡았던 실수들을 미리 피해서 설계함: 서로 다른 호출 결과를 섞어 비교하지 않기, 재계산과 원문을 직접 대조하기). 150~200시드 규모로 결정성·구조·사건 참조 정확성·수치 재집계 일치를 확인.
- **알아챈 사소한 엣지 케이스(수정 안 함, 정직하게 기록)**: 극히 드물게 "첫 구조물 철거"가 곧바로 넥서스까지 함락시키는 단 한 번의 공성으로 세트가 끝나면, 그 사건이 "(첫 구조물)" 라벨로만 표시되고 "(세트 종료)" 라벨은 중복 사건이라 생략된다(내용 자체는 여전히 정확 — 그 사건이 빠지는 게 아니라 라벨 우선순위만 다르다). 420회 표본에서 한 번도 발생하지 않을 만큼 드물어 이번엔 라벨 우선순위 로직을 더 복잡하게 만들지 않았다.
- tsc 0 에러, `npm run build` 성공.
- 8개 무관 스위트(`composition/ability/management/narration/replay/spatial/director/broadcast`) 전부 PASS(F14가 `SetResult` 반환 형태를 바꿨는데도 다른 영역 회귀 없음 확인 — 특히 `recap` 필드를 참조하는 코드가 `app/manager.tsx` 단 한 곳뿐임을 grep으로 먼저 확인하고 시작해 블라스트 반경을 미리 좁혔다).
- `tests/engine.test.mjs`: **PASS** — 3커리어, 1103 결정적 세트.
- `tests/combat.test.mjs`: **PASS**, exit 0 — 전 구간 이상 없음(승패·rng·골드 로직 무변경 재확인).

**기준선 대비 변화:** 없음(승패·자원·rng 호출 수 무변화 — recap 재구성은 이미 계산된 `events`/최종 `cs` 상태를 다르게 요약할 뿐 새 rng를 쓰지 않는다).

**알려진 문제·미검증·외부 차단:** 없음(BLOCKED 아님). "각 카드에 근거 사건으로 돌아가는 연결을 붙여라"의 UI 클릭 이동(카드 클릭 → 해당 사건으로 스크롤)은 이번에 구현하지 않았다 — 카드 문구에 실제 사건 번호(`#N`)를 명시해 사용자가 아래 이벤트 로그에서 직접 찾을 수 있게 하는 선에서 그쳤다(텍스트 연결은 완료, 클릭 상호작용은 별도 UI 작업). 브라우저에서 실제 렌더링 확인은 안 함(도구 제약, F02와 동일 범주).

**다음 작업 ID / 바로 실행할 첫 행동:** F01~F14(각각 첫 슬라이스 또는 완전 검증)까지 이번 세션에서 마쳤다. 다음은 P3(F16~F22, 시즌 서사 확대) 또는 F14의 남은 조각(리캡 카드 클릭→사건 스크롤 UI). 지시서 우선순위상 핵심 경기 루프(P0~P2)가 이제 전부 한 번씩은 다뤄졌으니, 다음 세션은 사용자에게 남은 우선순위를 확인하거나 P3로 자연스럽게 진행하는 것을 고려.
