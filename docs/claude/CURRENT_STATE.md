> 최신 후속: `BROADCAST_CONTINUATION.md` 우선. 현재 KDA·역할 진형·키프레임 상태 시각·완료 사건 공개·재생 제어 수정. Git 작업 없음.

> 2026-09-14 Codex 업데이트: `BROADCAST_UPDATE.md`를 먼저 읽을 것. D022 이후 공통 경로·도착, 조합 전투, 적 넥서스 재생을 구현했다. 다음 인수는 PC/모바일 브라우저 확인이며 과거 미완료 절차를 그대로 재구현하지 않는다.

# 현재 작업 상태 — 2026-09-09

## 기준과 이번 범위
기준은 사용자 수정 ZIP `PROTOCOL-Manager-Source(2).zip`이다. 이번 작업은 Claude Code 인수인계 설정과 문서를 추가하는 것이다. 기존 게임 소스/패키지 락/테스트/기획서의 바이트는 보존한다. 추가 파일 목록과 해시는 SOURCE_MANIFEST.json / PACKAGE_MANIFEST.json을 참고한다.

원본에는 56,477개 항목과 약 827MB의 비압축 내용이 있었다. 이 패키지는 개발에 필요한 소스만 포함하며 node_modules·빌드 결과·로컬 DB·런타임 캐시를 제외한다. 원본 업로드 파일은 수정하지 않았다.

## 사용자 작업본에서 확인된 구현
- 실제 챔피언 140종, 그림 URL/문자 폴백, 실존 팀·선수 표시 데이터.
- 직접 밴픽, 자유 픽, 포지션 최적 배정/스왑, 오프롤 −6.
- Lv0~4 숙련도/XP, 챔피언 특훈, 시즌 메타.
- 실제 챔피언을 쓰는 한국어 중계, 골드 그래프, 배속/결과 보기.
- 팀 운영, 국내/국제대회, 계약/스태프, 3슬롯 저장과 백업.

## 서로 다른 작업본을 섞지 말 것
이전 ChatGPT 작업 폴더에도 독립적인 v3 실험본이 있지만, 챔피언60종, mastery 객체, draftChoose 명령을 사용하여 지금 사용자 v3와 호환되지 않는다. 사용자 ZIP이 기준이다. 과거 파일을 통째로 덮어쓰지 않는다. 이 인수인계 직전 ChatGPT의 수정은 로컬 커밋까지만 되었고 라이브 배포하지 않았다.

## 실제 검증 결과
- `tests/management.test.mjs`: PASS. 업그레이드 멱등성, 재계약, 코치·심리 회복, 트레이드.
- `tests/engine.test.mjs`: PASS. 3구단 각각 4시즌, 총 1,115개 결정적 세트. 구단별 1,478/1,495/1,135개의 테스트 상위 루프 명령.
- 추가 직접 진행 확인: seed44/T1, 사용자 선택10번, 밴픽20단계 완료, 결과8이벤트.
- 타입 검사 결과: VALIDATION.md 참고.
- 이 검증은 사용자 ZIP에 들어 있던 엔진을 대상으로 했다. 이전 ChatGPT 버전의 1,057세트 결과와 혼동하지 않는다.
- Claude 설치/로그인, 사용자 Windows 실행, 로컬 DB 마이그레이션 실행, 브라우저·터치·라이브 배포는 미검증.

## 실제 코드에서 발견한 다음 개선점
1. ~~`train()`은 trainChamp가 이미 mastery에 있어야 그 챔피언을 훈련한다. UI는 미등록 챔피언도 고를 수 있어 다른 챔피언으로 대체될 수 있다.~~ → 2026-09-10 TRAIN-01(D006)에서 수정. 사용자 목표를 그대로 훈련하고, 풀이 가득 차면 명시적으로 거부한다.
2. `narrate()`의 첫 킬 표시는 초반 라인 이벤트에서만 갱신한다. 오브젝트 이후 첫 킬·세트8번째 이벤트 종료 등을 일관되게 표현하는 테스트가 필요하다.
3. 구버전 저장의 c0-0 같은 가상 챔피언 ID가 현재140개 ID로 변환되는 경로는 확인되지 않았다. 폴백으로 ID가 노출될 수 있다.
4. 밴픽 카드의 숙련도는 팀 전체 최대치여서 실제 배정 선수와 다를 수 있다. 단순히 별표를 보고 보너스가 적용된다고 오해할 여지가 있다.
5. 기존 README_KO/GDD에는 가상 구단·옛 리비전 등 현행 코드와 다른 표현이 있다.
6. rosters.ts의 현실 데이터는 근사치라는 코드 주석이 있다. 최신 실제 로스터 검증은 별도 작업이다.

이 목록은 이번 작업에서 수정했다고 주장하는 목록이 아니다. 자세한 완료 조건은 BACKLOG.md에 있다.
